/** Re-export shim — the settle-commit hysteresis gate moved to
 *  `@cityjson/navara-flatcitybuf` in M7.5. */
export { shouldRefetch } from "@cityjson/navara-flatcitybuf";
export type { CommitView } from "@cityjson/navara-flatcitybuf";
