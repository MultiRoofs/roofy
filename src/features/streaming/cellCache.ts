/** Re-export shim — the resident-cell LRU under triangle/byte budgets moved
 *  to `@cityjson/navara-flatcitybuf` in M7.5. */
export { CellCache } from "@cityjson/navara-flatcitybuf";
export type { CellCacheOpts, CellStats } from "@cityjson/navara-flatcitybuf";
