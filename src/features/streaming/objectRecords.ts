/** Re-export shim — the compact per-object records the FCB worker ships to
 *  the main thread moved to `@cityjson/navara-flatcitybuf` in M7.5. */
export { toObjectRecords } from "@cityjson/navara-flatcitybuf";
