# Fix wave 1 brief — Milestone 3 gate (after the Codex milestone review)

You are a FRESH Opus implementer. Working directory /data2/hideba/multiroof-viewer, branch develop (HEAD = the commit named in your dispatch). Read, in order: CLAUDE.md; the global constraints file /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-12-processing-toolbox-m3/global-constraints.md; the plan's front matter (Global Constraints, engine facts, design decisions (c), (e), (f), (h)) in docs/superpowers/plans/2026-09-12-processing-toolbox-m3.md; the two milestone reviews m3-review-src.md and m3-review-tests.md, the pair review pair-27-28-review.md (Task 28's section), and the gate report task-29-report.md — all in the workspace dir above; the ledger progress.md (grep "fix wave 1" for the adjudication and every ruling).

## Source findings (every one addressed)

S1 `src/features/processing/buildingProxy.ts` (~170): on scope "All" the footprint arm re-reads EVERY object of the source file; restrict the reader rows to the scoped parent-table ids (the same id list the identity check compares against) BEFORE the feature proxies are built, so a source that gained buildings cannot make Aggregate count buildings the loaded layer never had; the identity check rejects a changed scoped identity. Engine test + unit test.
S2 `src/insights/computedColumns.ts` (~41): `ADD COLUMN IF NOT EXISTS` keeps a replaced computed column's OLD type. When a run replaces a computed column whose declared type differs (e.g. DOUBLE → BOOLEAN, or → VARCHAR), migrate it inside the write transaction (back it up as today, `DROP COLUMN` then `ADD COLUMN <declared type>`), keep the provenance/column metadata consistent, and make Undo restore the ORIGINAL type from the backup; apply the same to inherited derived-layer columns. Real-engine regression in tests/integration/duckdb/computedColumns.test.ts (type change both ways, values verified, Undo restores type and values); a source-attribute column is still never replaced.
S3 `src/ui/processing/useToolForm.ts` (~125) and the execution preflight: a mixed polygon/point GeoJSON layer must be treated as "polygons" only where §7.5/§7.6 require areas — the form's eligibility (`polygonOnly`) AND the queue head preflight skip non-polygon features (counted as skipped, "Needs areas (polygons)" only when NO polygon exists), so Join cannot select a coincident point as an area and Aggregate never writes counts onto points. Tests at both sites.
S4 `src/features/processing/tools/distanceToNearest.ts` (~127): bound the distance join — prefilter candidates by an envelope expanded by the max distance (`ST_Intersects(ST_Envelope-based box, source geom)` or equivalent) so the per-building candidate set is spatially bounded, reduce to the nearest candidate (min distance, ties by idx) BEFORE joining the source properties for the nearest id; keep `ST_Distance_GEOS` and the source-order tie rule; the engine probe pins identical answers to the previous shape on the fixture, plus a case where the prefilter excludes a far source.

## Test findings (from m3-review-tests.md; every MAJOR and the minors below)

M1 scenario 11 through the REAL Aggregate executor and builders: Selected(2) over six valid areas; all six survive, exact counts incl. zero, unselected buildings excluded, the parent unchanged. M2 a multi-LoD root+parts fixture prepared through the REAL table FIFO: retained geometry/attributes, excluded siblings, then Measure solids on the copy. M3 cancel AFTER publication for BOTH new-layer branches ("finished before the cancel arrived", Undo works) + a vector-parent relink during pending computation (fails, no copy, no provenance, replacement document unchanged, FIFO progresses). M4 death during UPDATE for This layer and New layer: the issued statements survive in the log (Task 27's fix round did this — verify and extend if missing). Minors: 5 (crossLayerRun.test.ts:532 asserts done + the vector property + no city write), 6 (multiple contributing footprints + root fallback; min/max + mixed-NULL aggregates through buildAggregateSql), 7 (hold the FIFO, remove the source, release: refusal + a following run; Join/Distance and Aggregate's reversed direction), 8 (nextTableName interleaved with a real build), 9 (validateSolids reverses the scope's parts), 10 (arm cancellation after encoding completes; timer-delivered cancellation during row processing in Join/Distance), 12 (reset provenance between cases; restore the patched prototype).

## Docs corrections (from pair-27-28-review.md, Task 28)

docs/roadmap.md ~660/701: narrow "every engine await settles / nothing hangs" to the six primitives; queryParquetBuffer's unraced operations are REGISTRATION and CLEANUP (its query is raced); "return a message" overstates Stats/map-filter. docs/architecture-notes.md ~643 (D10): ST_DWithin returns TRUE for polygons 40 m apart at a 39 m threshold (a boolean false positive), ST_Distance returns 0 — distinguish. Minors: "No toolId appears" → "no tool-specific branching"; qualify D2 by the unloaded-json condition; the source-byte lifetime (through the reader statements, released before the roll-up). Also document S1–S4's consequences briefly in the M13.3 section (the scoped reread, the typed replacement migration, polygon-only eligibility, the bounded distance join).

## Gate smoke defects

Whatever task-29-report.md lists as defects (file/symptom/screenshot): fix each, or state why not with a ruling request.

## Rules

TDD red-first per finding; Node 24 shims; focused tests while iterating; `DUCKDB_INTEGRATION=1` for engine cases; the full app suite ONCE in the background before the final commit (`& wait $!; echo "suite: $?"`); `npx vp check` 0 errors / 56 warnings; `npx tsc -b --noEmit`; one finding per commit where practical, `fix:`/`test:`/`docs:` prefixes; NO trailers of any kind; never bypass hooks; do not push; do not stage `.github/hooks/`, `docs/design-history/`, `.superpowers/`; do not edit the plan. Write /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-12-processing-toolbox-m3/fixwave-1-report.md (finding → commit → evidence) and reply with the short status contract.

## Gate smoke defects (task-29-report.md) — rulings

F1 CRITICAL `src/features/processing/solidSql.ts`: `ST_3DSurfaceArea` (and possibly `ST_3DFootprintArea`) RAISE on a solid with a degenerate face, and one such row aborts the whole statement (the Delft LoD 2.2 run). Engine fact D11 (probed by the commander): DuckDB's `TRY()` exists in 1.5.5 but does NOT catch three_d's "Invalid Error", so the guard must come from the validation report — `CASE WHEN s IS NOT NULL AND r.degenerate_face_count = 0 THEN ST_3DSurfaceArea(s) END` (same for footprint; probe which other functions raise on such a solid and guard those too). Do NOT use `is_valid` for the areas (scenario 2B proves an invalid solid still needs its envelope). Report such solids as a caveat (`{cause: "degenerate faces (no area)", count}` — or the wording the plan's copy table allows; if none, propose it as adapted copy A18 in your report and use it tagged [adapted copy]). Build an in-test degenerate solid (one zero-area face) for the probe suite, pin which functions raise, and pin the guarded statement's answer; re-run the Delft LoD 2.2 shape in the probe if the fixture allows.
F2 `runQueue.ts` `summarise`: "1 invalid solids (no volume)" → singular at count 1 (every caveat/skip cause with a count noun follows the same rule; check the roof/extent lines are unchanged).
F3 `RunFooter.tsx`: Style by result is live but inert on an UNDONE card → disabled (with the existing disabled shape) once the run is undone; test.
F4 (seen once, not reproduced): provenance cleared without the stale mark, blocking a same-prefix re-run — read the rebuild/provenance paths (`clearLayer`, the stale watcher, the streaming sweep) for a path that clears provenance without marking the run stale; if found, fix with a regression; if not, record it as unreproduced in your report with what you checked.
F5 MAJOR `layerCoordination.ts` rules 1+2: a Selected-scoped VECTOR-target run is unreachable because choosing the vector target clears/blocks the city selection. Ruling: §6 says "Changing the target does not change the active layer" — Aggregate's Selected scope freezes the SOURCE city layer's selection; picking the vector target in the form must not clear it, and Run must be enabled with "Selected N" counting the source layer's selection. Fix the coordination rule (narrowly), test it, and make scenario 11's Selected(2) path pass in the M1 test (the reviewer's M1 above covers the executor side).
