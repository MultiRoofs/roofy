You are the MILESTONE reviewer for Roofy's processing toolbox Milestone 3 (M13.3): the SOURCE pass. A second reviewer covers the tests. Read, by absolute path, in chunks:

1. The spec (authority): /data2/hideba/multiroof-viewer/docs/superpowers/specs/2026-09-10-processing-toolbox-design.md — §6 (OUTPUT, "What a derived layer is"), §6.1–6.4, §7 (common rules), §7.2, §7.3, §7.5, §7.6, §7.7, §8, §10 scenarios 2, 3, 5, 8, 10, 11, 12.
2. The hard rules: /data2/hideba/multiroof-viewer/CLAUDE.md.
3. The plan's front matter and "Decisions recorded": /data2/hideba/multiroof-viewer/docs/superpowers/plans/2026-09-12-processing-toolbox-m3.md (everything before "## Tasks", and the two closing sections). The owner's decisions and the commander's rulings there are binding; do not re-open them.
4. The ledger's rulings and engine facts: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-12-processing-toolbox-m3/progress.md (grep "Ruling", "D1", "D10", "parked", "carried").
5. The whole-milestone diff, SOURCE files only (src/ and packages/): {DIFF} — read it to the end in chunks. It is large; budget your reading: the run queue, the executors, the SQL builders, the derived-layer and vector-results modules, duckdb.ts, and the form/footer are where the risk is.

Review the SOURCE for: (1) correctness against the spec and the rulings (features not rows; contributors; NULL rules; the guarded three_d SQL; ST_CoveredBy; ST_Distance_GEOS; the typed values read; the one-statement identity check; derived layers cut from the parent table; snapshot omission; the New-layer dispatch before every This-layer publication; the engine-death paths); (2) regressions in M1/M2 behaviour (Height from extent, Roof metrics, the table/Details/rules/export surfaces, the streaming lifecycle); (3) resource lifecycle — every VFS buffer and per-run table released on every exit path, the FIFO never stranded; (4) hard-rule violations (duckdb.ts sole importer, ONE status writer, retryEngine the door, no @navaramap outside the named modules, brand tokens); (5) copy — every user-visible string verbatim from the spec or the accepted A1–A17; (6) anything unbounded (statement size, memory) on a 100k-feature layer. Number findings, tag CRITICAL / MAJOR / MINOR, give file:line and a concrete fix. Be terse. End with a one-line verdict "Ship" / "Not yet".
