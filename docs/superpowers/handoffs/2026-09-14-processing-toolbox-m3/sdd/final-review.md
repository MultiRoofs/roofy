1. **Important — partial-scope type migration erases unrelated values.** [computedColumns.ts:310](/data2/hideba/multiroof-viewer/src/insights/computedColumns.ts:310) drops the entire column, recreates it, then updates only scoped rows. Outside-scope values become NULL while model attributes retain their previous values, violating §7. The [engine test:911](/data2/hideba/multiroof-viewer/tests/integration/duckdb/computedColumns.test.ts:911) explicitly expects this loss. **Fix:** preserve outside-scope values through lossless migration; reject the replacement atomically when that is impossible. Correct the regression to assert table/model agreement before Undo.

2. **Important — vector-source relinking does not prevent publication.** [runQueue.ts:1491](/data2/hideba/multiroof-viewer/src/features/processing/runQueue.ts:1491) proceeds from computation to publication without checking the captured vector source. Relinking Zones during Join or Distance therefore publishes results from its previous document, for either destination. Existing identity checks protect vector **targets** only; the watcher checks removal only. **Fix:** capture source identity and invalidate the run on relink through the commit/publication boundary. Test both destinations, including relink during computation and during write preparation; assert cleanup and FIFO progress.

The binding parked/carried decisions do not independently warrant reopening here. Tests were sampled, not rerun.

**With fixes** — one last wave: scope-safe type migration, vector-source relink protection, and the focused regressions above.
