You are RE-REVIEWING an IMPLEMENTATION PLAN after an amendment round (round 2 of 2, the last): Roofy processing toolbox, Milestone 3 (M13.3). It is large (about 24,000 lines, 29 tasks). You review ONE strand of it; two other reviewers cover the rest in parallel. Read, by absolute path, in sequential chunks:

1. The spec (authority): /data2/hideba/multiroof-viewer/docs/superpowers/specs/2026-09-10-processing-toolbox-design.md — the sections named for your strand below, plus §6, §6.1, §6.2, §7 (common rules) and §8 for everyone.
2. The hard rules: /data2/hideba/multiroof-viewer/CLAUDE.md.
3. The plan under review: /data2/hideba/multiroof-viewer/docs/superpowers/plans/2026-09-12-processing-toolbox-m3.md. Read its FRONT MATTER in full (everything before "## Tasks": Global Constraints, the verified engine and code facts, the "New exported names" ledger, the File map, the ten Design decisions, the copy table) and its two closing sections ("## Decisions recorded (2026-09-12)" — the repo owner has ALREADY answered every question there; do not re-open them — and "## Self-review"). Then read ONLY the task bodies of your strand, each from its "### Task N:" heading to its last commit step (grep -n "^### Task" to find the line ranges). Skim the headings and Interfaces blocks of the other tasks only where your tasks consume or produce something for them.
4. Spot-check at least six of the plan's file:line citations that your tasks rely on against this checkout (read-only) and report any that are wrong.
5. YOUR OWN ROUND-1 FINDINGS: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-12-processing-toolbox-m3/plan-review-C.md — for EACH numbered finding say Resolved / Partly / Unresolved with the plan line where the fix now lives (line numbers have moved; grep for the task heading). The commander's rulings on the parked ones are in the plan's 'Decisions recorded' and in /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-12-processing-toolbox-m3/progress.md (grep Ruling) — do not re-argue a ruling, but say if a ruling breaks a spec sentence. Then look for REGRESSIONS the amendments introduced and for NEW cross-strand inconsistencies (a name or signature your tasks consume from another strand that the front matter's ledger now defines differently). 6. The M2 handoff ledger for the rulings that bind M3: /data2/hideba/multiroof-viewer/docs/superpowers/handoffs/2026-09-12-processing-toolbox-m2/sdd/progress.md (grep "Ruling").

Review for: (1) spec coverage of your strand and honest labelling of what is unreachable or deviated; (2) whether the design decisions your tasks implement hold against the spec, the hard rules and the code as it is (read the source files the tasks modify); (3) interface consistency ACROSS tasks — a name, signature or type used in a later task must be defined identically in an earlier one or exist in the code today; (4) test quality — red-first tests that assert behaviour, not mocks; complete, importable test files (mocks present, fixtures present); (5) placeholders or hand-waving ("add validation", "similar to Task N", elided code) — each is a defect; (6) SQL — every statement must be one the plan's verified engine facts support (ST_3DTryFromWKB never ST_3DFromWKB; ST_3DVolume only under a CASE on is_valid; footprints only from LoD 0 columns; the median CAST to DOUBLE over root rows); (7) copy — every user-visible string verbatim from the spec or one of the accepted [adapted copy] A1–A16 strings, nothing else; (8) risks the plan underestimates (memory, the table FIFO, cancellation, the engine-death paths, noUncheckedIndexedAccess). Number findings, tag CRITICAL / MAJOR / MINOR, give the task number and plan line numbers and a concrete fix. Be terse. End with a one-line verdict "Execute" / "Fix first".

YOUR STRAND: Tasks 20–29 (the OUTPUT destination and Name rule, derived city layers, the destination branch with publication and Undo, derived vector layers, the derived layer in the list/snapshot/export, engine death off the table FIFO, palette rotation and Color-by-at-Save, the M1/M2 leftovers, documentation, the milestone gate). Spec sections: §3, §6 (OUTPUT, Write to, Name, "What a derived layer is"), §6.1 (publication as the last step), §6.2 (the done card for a New-layer run, Undo, Zoom to layer, Open table scroll), §6.4, §8 (Persistence), §10 scenarios 5, 10, 11, 12. Also read: src/insights/layerTables.ts (enqueue, the FIFO, retryEngine), src/insights/duckdb.ts (runQuery, ddl, registerBuffer, dropBuffer, markEngineDead, onEngineDeath), src/features/processing/runQueue.ts (undoRun, installStaleWatcher), src/features/layers/layerStore.ts (addLayer), src/persistence/captureSnapshot.ts, src/app/App.tsx (the snapshot serialisation), src/ui/processing/RunFooter.tsx, src/scene/cityColors.ts.
