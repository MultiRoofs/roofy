You are the milestone reviewer for Roofy's processing toolbox, Milestone 1 (M13.1), on branch develop. This is pass "tests" of a two-pass review (the other pass covers the source diff); report only on what this pass shows.

Read, by path:

1. The spec (authority): /data2/hideba/multiroof-viewer/docs/superpowers/specs/2026-09-10-processing-toolbox-design.md — sections 4, 5, 6, 7 (common rules), 7.4, 8 and acceptance scenario 1 in §10. Milestone 1 ships scenario 1 (Height from extent on a static CityJSON layer, destination "This layer" only); New layer, LoD, extensions, cross-layer tools, streaming Details/rules are later milestones.
2. The hard rules: /data2/hideba/multiroof-viewer/CLAUDE.md (Hard Rules section).
3. The diff for this pass: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-10-processing-toolbox-m1/m1-diff-tests.diff (git diff 0d787fb..HEAD over tests/, scripts/smoke/ and docs/). It is large; read it in sequential chunks until the end. Do not run git commands; read-only on this checkout; inspect files outside the diff only for a concrete named risk (one focused check per risk, named in your report). Do not dispatch subagents.
4. The ledger of rulings and deferred minors the controller already made: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-10-processing-toolbox-m1/progress.md (lines starting "Ruling:" or containing "parked" / "minor (deferred)"). A ruling there is a decision the human will read; do not re-litigate it, but DO flag it if the code contradicts the ruling or the ruling contradicts the spec in a way that breaks scenario 1.

Report correctness bugs, spec deviations (§4–§8, §7.4, scenario 1), missing or hollow tests, and hard-rule violations from CLAUDE.md (duckdb.ts sole importer of @duckdb/duckdb-wasm; ONE writer of the DuckDB status; @navaramap imports only in the named engine-binding modules; submodule-first; test files import from "vitest"; every user-visible string verbatim from the spec). Number findings, tag each CRITICAL / MAJOR / MINOR, give file:line and a concrete fix. Be terse: one line per finding plus the fix. End with a one-line verdict: "Ship" or "Not yet" and why.
