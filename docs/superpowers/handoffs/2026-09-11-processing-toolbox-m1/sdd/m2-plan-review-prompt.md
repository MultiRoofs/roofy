You are reviewing an IMPLEMENTATION PLAN before it is executed: Roofy processing toolbox, Milestone 2 (M13.2). Read, by path:

1. The spec (authority): /data2/hideba/multiroof-viewer/docs/superpowers/specs/2026-09-10-processing-toolbox-design.md — §5 (chips, extension tooltips/failure copy/Retry), §6 (LoD select, PARAMETERS validation, extension note), §6.1 (phases), §7 common rules, §7.1 Roof metrics, §8, §10 scenarios 4 and 6.
2. The hard rules: /data2/hideba/multiroof-viewer/CLAUDE.md (especially "ONE writer of the DuckDB status", "retryEngine() is the door", the duckdb.ts sole-importer rule, submodule-first).
3. The plan under review: /data2/hideba/multiroof-viewer/docs/superpowers/plans/2026-09-11-processing-toolbox-m2.md (3,640 lines; read it in sequential chunks to the end).
4. For its "Facts about the current code" claims, spot-check at least eight of the file:line citations on this checkout (read-only) and report any that are wrong.
5. The M1 plan for the house style and the seams M2 must consume: /data2/hideba/multiroof-viewer/docs/superpowers/plans/2026-09-10-processing-toolbox-m1.md (skim), and M1's ledger rulings that bind M2: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-10-processing-toolbox-m1/progress.md (grep "Ruling:", "M2", "parked").

Review for: (1) spec coverage of the M2 scope (Roof metrics §7.1 in full, lazy extension loading + chip states + Retry, the status subscription) and honest labelling of what is unreachable; (2) the three design decisions — does each hold against the spec and the hard rules, and is the CLAUDE.md rule change exact and justified; is the streaming-in decision (a submodule commit LoD-tagging resident roof metrics) sound and small, or should streaming be excluded; is the app-side computation from Surface.type/lod + computeRoofMetrics correct for §7's contributor and roll-up rules (area-weighted slope, flat share, dominant azimuth of the largest non-flat surface, count), and are the `aggregate.ts` reuse rejection reasons true; (3) task ordering and interface consistency across tasks (names, signatures, types used in later tasks defined in earlier ones); (4) test quality — red-first tests that assert behaviour, not mocks; the `useSyncExternalStore` over a version counter mocking argument; (5) placeholders or hand-waving ("add validation", "similar to task N") — each is a defect; (6) copy — every user-visible string verbatim from the spec, and the plan's proposed non-spec sentences clearly flagged for the human; (7) risks the plan underestimates. Number findings, tag CRITICAL / MAJOR / MINOR, give plan line numbers and a concrete fix. Be terse. End with: which open questions in the plan's final section you would answer and how, and a one-line verdict "Execute" / "Fix first".
