# SDD ledger — plan: docs/superpowers/plans/2026-09-10-processing-toolbox-m1.md

Spec: docs/superpowers/specs/2026-09-10-processing-toolbox-design.md (read).
Branch: develop (CLAUDE.md: commit directly on develop, no feature branch).
Milestone start SHA: 0d787fb (before plan commits); plan committed at c4dd549.
Submodule pin: e681c18, detached HEAD.

Ruling: work on `develop` directly, no worktree — CLAUDE.md mandates committing on develop and the user chose this workflow; cost if wrong: an unfinished milestone sits on the shared branch (unpushed until the milestone gate).
Ruling: implementers run on Opus (user instruction: "use Opus as implementor"); task reviewers run on Sonnet; the milestone review is Codex gpt-6-astra (user instruction) in addition to the skill's final whole-branch review — cost if wrong: higher token cost per task.

## Pre-flight scan

| Tasks          | Shared surface                                                                                                                                          | Produces vs consumes                                                                                                                | Finding / ruling                                                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1 → 2,3,4,9,11 | `types.ts` (`RunRecord`, `ToolId`, `Scope`)                                                                                                             | Task 2 store, Task 3 button and Task 9 queue use `RunRecord` fields `status`, `undoable`, `stale`, `note`, `phase`                  | consistent (all fields declared in Task 1)                                                                                               |
| 2 → 4,9,11,13  | `processingStore` (`open`, `view`, `drafts`, `runs`, `notice/noticeSeq`, `upsertRun`, `patchRun`, `pushNotice`, `runById`)                              | Task 9 uses `patchRun`/`upsertRun`/`runById`; Task 13 subscribes to `noticeSeq`; Task 4 adds a shell-store side effect in `setOpen` | consistent; Task 4 step 6 amends Task 2's `setOpen` (documented)                                                                         |
| 3 → 4          | `App.tsx` map header                                                                                                                                    | Task 3 inserts `ToolsButton`; Task 4 edits the right panel props                                                                    | different regions of App.tsx; sequential, no conflict                                                                                    |
| 4 → 11         | `ToolView.tsx`, `LogView.tsx`, `RecentRuns.tsx` placeholders                                                                                            | Task 4 creates placeholders; Task 11 replaces them                                                                                  | intended                                                                                                                                 |
| 5 → 10         | flat rows `bbox` STRUCT                                                                                                                                 | Task 10 SQL reads `"bbox"."zmin"`                                                                                                   | field names `xmin…zmax` match the reader's; ruling: fallback bbox is the object's OWN extent, roll-up covers parts (spec §7.4 corrected) |
| 6 → 9          | `runOnTableQueue`                                                                                                                                       | Task 9 wraps `execute` and `undo` in it                                                                                             | consistent                                                                                                                               |
| 7 → 9,12       | `mergeAttributes`, `setModel`                                                                                                                           | Task 9 calls `mergeAttributes` with `undefined` to delete on undo; Task 7 test covers undefined-deletes                             | consistent                                                                                                                               |
| 8 → 9,11,12    | `writeComputedColumns`, `undoComputedColumns`, `useComputedColumnStore`, `computedColumnsOf`, `provenanceOf`, `formatProvenance` (Task 12 adds)         | Task 9 uses write/undo/store; Task 11 form uses `computedColumnsOf`; Task 12 adds `formatProvenance`                                | consistent                                                                                                                               |
| 9 → 10,11      | `ToolContext`, `ToolResult`, `ToolExecutor`, `registerExecutor`, `submitRun`, `cancelRun`, `undoRun`, `refreshLayerTableColumns`, `buildFeatureRowsSql` | Task 10 registers an executor; Task 11 calls submit/cancel/undo                                                                     | consistent; `tools/register.ts` import side effect ruled in Task 10                                                                      |
| 9 (self)       | scope test rows `{id, f}`                                                                                                                               | step 1 text first shows `{ id }` rows then corrects to `{ id, f }`                                                                  | Ruling: the `{ id, f }` form with `buildFeatureRowsSql` is binding (the later paragraph) — cost if wrong: none, test shape only          |
| 11 (self)      | `RunFooter`, `RecentRuns`, `LogView` are specified in prose, not code                                                                                   | plan rubric prefers code                                                                                                            | Ruling: prose is precise enough (labels, actions, states named); implementer follows spec §6 — cost if wrong: a review round             |
| 12 → 8         | `ComputedAttributeBadge` gains `title` prop                                                                                                             | existing callers keep the default                                                                                                   | consistent                                                                                                                               |
| 13 → 2, 11     | toast via `noticeSeq`; Style by result draft                                                                                                            | Task 11's RunFooter defers the draft to Task 13                                                                                     | consistent                                                                                                                               |
| Global         | new `duckdb.ts` exports                                                                                                                                 | none planned in M1                                                                                                                  | consistent                                                                                                                               |

## Task log

Task 1: implemented (commit 14701ca), DONE*WITH_CONCERNS: pre-existing failures on develop block pre-push — appCityParquetLayers.test.tsx (drop-zone testid) and vp check error DrawOverlay.tsx:130; vitest must run under mise Node 24 shims.
Ruling: commit trailers — none (session instruction overrides CLAUDE.md's Co-Authored-By line) — cost if wrong: trailers absent from history.
Ruling: the two pre-existing develop failures are fixed as a separate chore commit at the milestone gate (Task 14) so pre-push passes; not folded into feature tasks — cost if wrong: a slightly larger gate.
Task 1: complete (commits c4dd549..14701ca, review clean)
Task 2: implemented (commit 12a602e); note: noUncheckedIndexedAccess means later briefs' verbatim tests may need ?. on array index
Task 2: review Approved with 2 Important plan-mandated findings: (a) openTool/openLog open the panel without clearing unseenFailure; (b) patchRun raises unseenFailure for an unknown run id. Ruling: both real; carried into Task 4's dispatch (Task 4 already amends processingStore.setOpen) instead of a parallel fix dispatch while Task 3 runs — cost if wrong: Task 4 grows by two small edits + tests.
Task 2: minor (deferred): no tests for back() from a log view, openLog while closed; resetForTest has no repo precedent.
Task 2: complete (commits 14701ca..12a602e, review approved; 2 findings carried to Task 4)
Task 3: implemented (commit 2d55992); browser-checked via Playwright; peers 32px vs 30px pre-existing drift noted
Task 3: complete (commits 12a602e..2d55992, review clean; minor: brief listed flatControls.css unused)
Task 4: implemented (commits d6ebf06, 7b128d2); agent stalled after writing the report (watchdog) — treated as DONE
Ruling (user, 2026-09-11): pushing develop is authorised; the pre-existing pre-push blockers (appCityParquetLayers.test.tsx drop-zone testid; DrawOverlay.tsx:130 no-unused-expressions) are fixed as a chore commit right after Task 5 (sequenced, never parallel with an implementer); push after that and after each reviewed task from then on.
Task 4: review Approved; Important: ToolsButton closes the toolbox when the Details tab is showing instead of switching to Tools (spec §4.1). Ruling: real; fix carried into Task 11's dispatch (lift `tab` into processingStore as `activeTab` + `setTab`; ToolsButton switches/expands and only closes when Tools is visibly active; openTool/openLog also un-collapse the panel) — cost if wrong: Task 11 grows; a tool form can be lost to the button until then.
Task 4: minor (deferred → Task 11): rename the misnamed Escape test (closed toolbox on a tool view clears the selection) and add sheet-before-form coverage; remove dead `--font-label` (use --font-mono) and unused `data-extension`/`.processing-catalogue` hooks or give them consumers; tab widget a11y (aria-controls/tabpanel) and `aria-disabled` on clickable rows revisited with the tool form.
Task 4: complete (commits 2d55992..7b128d2, review approved; 1 Important carried to Task 11)
Task 5: implemented (commits 8bfd3c1, eb9a5b4); also added FLAT_COLUMN_TYPES.bbox in layerTables.ts (ALTER loop needs a type); integration suite 20/20
Task 5: minor (deferred): make FLAT_PREFIX_COLUMNS a const tuple keyed into FLAT_COLUMN_TYPES so a missing type is a tsc error; mixed-null bbox inference untested; no parsed-model→row bbox test.
Task 5: complete (commits 7b128d2..eb9a5b4, review approved)
Chore: 0d035ea fixes the two pre-push blockers; pushed develop 0d787fb..0d035ea (spec, plan, mockup, Tasks 1–5)
Task 6: first implementer stalled in the full-suite run (watchdog); the pending 40-line diff (runOnTableQueue + 2 tests) was read in full by the controller and matches the brief exactly; a fresh implementer commits/pushes it. Ruling: controller review suffices for this diff (read completely; FIFO ordering and rejection tests assert behaviour) — cost if wrong: an unreviewed 6-line function.
Note: index.html carries an uncommitted launch-screen tagline edit made outside this run (not by any agent); left untouched, surfaced to the user.
Task 6: complete (commit af6534e, controller-reviewed); pushed 0d035ea..af6534e (index.html WIP stashed around the push and restored)
Task 7: implemented (submodule a890c63 → origin/main; parent a4c1218); pushed af6534e..a4c1218 with the hook's suite green (2661) — implementer's 38 'Node-24 localStorage' failures were an env artefact
Task 7: minor (deferred): handleSync.test.ts:307 asserts setModel arg by deep equality (use toBe); repaint() lacks a null-evaluator early-out (plugin; add `if (!this.evaluator) return` — safe); mergeAttributes re-mints a model for no-op patches; `as unknown as CityModel` cast in the plugin fixture.
Task 7: complete (commits af6534e..a4c1218 + plugin e681c18..a890c63, review approved)
Task 8: implemented (commit d673991); pushed a4c1218..d673991. Concerns carried to Task 9: empty rows must short-circuit before writeComputedColumns; read_json_auto inference on all-NULL columns to probe at the milestone smoke
Task 8: review Approved with 4 Important plan-inherited items. Rulings: (1) empty-rows guard lives in Task 9 (already in its dispatch); (2)(3) read_json_auto inference (all-NULL / BigInt-as-string / sample_size) and ALTER-then-UPDATE-in-one-transaction are PROBES against real DuckDB 1.5.5 in the Task 14 gate using the DUCKDB_INTEGRATION=1 harness; if the transaction order is refused, split into ALTER outside + UPDATE inside and document; (4) the shared connection has no other BEGIN user (grep-confirmed) and runs are serialised on the table FIFO; concurrent reads inside the transaction are harmless — accepted for M1, noted in architecture-notes at the gate — cost if wrong: a probe failure at the gate forces a rework of writeComputedColumns.
Task 8: minor (deferred): `{ ...row, id }` ordering so an output column named id cannot clobber the key; runtime whitelist of ColumnType; full-order assertion in the write test; removeColumns on an untouched layer mints an empty entry; computedColumnsOf returns a fresh Set (UI must not use it as a selector snapshot).
Task 8: complete (commits a4c1218..d673991, review approved)
Task 9: implemented (b8d9f85, dc088cd, 17a6b9f); implementer concern: scope inputs read at queue head not frozen at Run (spec §6.1) → fix round after review
Task 10: implemented (66d5f89, 368de10); pushed
Task 9: review Needs fixes — Important: (1) undoRun's refreshLayerTableColumns runs outside the queued task (Undo→Run again reads a dropped column; rebuild race clobbers registry); (2) Provenance.partial counts rows not features; plan-mandated (3) scope resolved at queue head, not frozen at Run. Ruling on (3): freeze the scope INPUTS at Run (selection object ids for `selected`, the applied FilterGroup for `matching`) in submitRun and resolve at the head from that snapshot; a queued run whose target table was rebuilt fails with the §6.1 reason — cost if wrong: ids resolved slightly later than the spec's letter, but the goalposts cannot move.
Task 9: fix round 1/5 dispatched (findings 1–3 + minors: abort check after scope; drop \_\_undo* tables when a run's Undo is taken away or it leaves the history; tests for "finished before the cancel arrived" and the Undo takeover; elapsedMs on early failures).
Task 10: minor (deferred): register.test.ts pins EXECUTORS to exactly ["height-from-extent"] (edit when the next tool registers); zmin>zmax corrupt bbox writes a negative height (decision: honest report, no guard); Task 11 must build RunRequest.columns from the same prefix the executor uses.
Task 10: complete (commits 17a6b9f..368de10, review approved)
Task 9: fix round 1/5 (8 addressed per implementer; commits 0dd69e4, fd8a6f8, 63eb06e); pushed through 63eb06e; external launch-screen WIP (index.html, launchScreen.ts, launchScreen.test.ts) stashed around the push and restored
Task 9: fix round 1/5 (8 addressed, 0 open; commits 368de10..63eb06e; re-review clean, no new breakage)
Task 9: minor (deferred): backup-table cleanup on 21-run eviction untested; matching/selected scopes can now be refused by a failed feature-count query.
Task 9: complete (commits d673991..63eb06e, review clean after 1 fix round)
Task 11: implemented (a1b1e39, bbba686, c9be262, 2df6d33) incl. Task 4 carried items A/B/C; browser-verified end to end; pushed. Concern → Task 12: computed columns must be in the drawer's default visible column set (spec §8)
Task 11: review Needs fixes — Important: (1) failed card's Retry calls the form's run() (dead when invalid; re-runs the draft not the frozen params) → use submitRun(requestFromRun(run)); (2) "Queued behind X" shown before anything is queued → gate on latestRun.status === "queued"; (3) plan-mandated: §6.2 "Run again unlocks the form" — Ruling: follow the spec's letter: the form stays locked while the latest run's done/failed card shows; Retry re-runs the frozen request; Run again unlocks the form (returns the footer to idle with the same values) without submitting — cost if wrong: one extra click to re-run; (4) OUTPUT*COLUMNS duplicated from the executor with no equality test → export the name builder from the tool module and consume it in both; (5) eligibleTargets filters on table-ready only, not tool eligibility → filter with toolEligibility per layer. Minors 6–9 folded (matching positive test; duplicate .processing-back rules + 30px magic number; queued footer reason precedence; aria-live on the progress block, warnings keyed by index). Fix round 1 dispatches after Task 12 commits (no parallel implementers).
Task 11: minor (deferred, cross-task → Task 14 gate): startedAt set at submit vs elapsed measured from execute (live timer jumps back); scopeCount 0 in a queued run's log header; §6.1 re-validation of a column that now belongs to the file is not implemented.
Task 12: implemented (e3cf64f); browser-verified; review NOT yet run (handover)
Takeover (2026-09-11, new commander session): workspace re-created from the committed handoff snapshot (docs/superpowers/handoffs/2026-09-11-processing-toolbox-m1/sdd). HEAD 997c869 (launch-screen WIP has been committed by the user; no stash dance needed unless new dirty files appear). Submodule working tree was at e681c18 while the parent pins a890c63 → checked out a890c63 (no local changes lost). Untracked `.github/hooks/`, `docs/design-history/` are someone else's; never staged.
Ruling: user instruction this session — implementers Opus, task/re-reviews Codex gpt-6-astra where practical (claude -p --model opus fallback per memory), Fable commander only; cost if wrong: token cost.
Ruling (amends Task 11 ruling on §6.2): the form locks only while a run is queued/running/cancelling or while the latest run's DONE card shows; a FAILED card (Retry + Log only, §6.3) never locks the form, so the user can edit and re-run after a failure; Retry re-runs the frozen request via submitRun(requestFromRun(run)); "Run again" dismisses the done card for that run (ToolView-local `dismissedRunId`, so the footer returns to idle with the same values, nothing submitted; the card returns if the user navigates away and back, which the spec does not forbid) — why: the prior ruling deadlocked on a failed run; cost if wrong: a card that reappears after navigation, or one extra click to re-run.
Ruling: commit trailers — none, not even the harness's Claude-Session line: the user's instruction this session ("no attribution trailers") is explicit and repeats the earlier session ruling — cost if wrong: session links absent from history (surfaced in the final message).
Task 11: fix round 1/5 dispatched to a FRESH Opus implementer (the original is gone with the previous session); BASE 997c869.
Task 12: review dispatched to Codex gpt-6-astra over review-2df6d33..e3cf64f.diff (brief + dispatch note: computed columns visible by default in the drawer).
Task 12: review (Codex gpt-6-astra) Needs fixes — Important: (1) a customised column list never gains later computed columns (TablePanel.tsx:236/243) — real, enters the fix loop: Open table appends the run's output columns to the layer's visible column order when one is set (no duplicates; the user can hide them afterwards), plus a customised-list test; (2) streaming (FCB) layers get the table badge but no Details/rules entries because runQueue merges only into `layer.model.objects` (empty for FCB) — Ruling: PARKED, deferred past M1: M1's goal is scenario 1 on static layers; the streaming attribute path lives in the FCB plugin/worker and is its own change (recorded in the roadmap 13.1 entry as open, carried to the M2 plan) — cost if wrong: a streaming run shows values in the table only until then; (3) "Computed" vs the spec's "COMPUTED" (Details sub-heading + rules optgroup), plan-mandated sentence case in the brief — Ruling: the spec wins (global constraint: every user-visible string verbatim): both labels read "COMPUTED"; the Details sub-heading uses the same mono label style as the toolbox's own group labels in the same panel (`.processing-group__label`), overriding workspace.css's sentence-case reset for this heading only — cost if wrong: one uppercase heading beside sentence-case peers, a one-line revert. Minor folded into the round: buildings-mode collision test (a computed column named like a base column appears once, last).
Task 12: fix round 1/5 queued behind the Task 11 fix implementer (no parallel implementers).
Task 11: fix round 1 implemented (67042c6..757baab, 9 commits), DONE_WITH_CONCERNS. Ruling (amends the dismissal mechanism): the done-card dismissal is STORE-level — processingStore `dismissedRunIds` + `dismissRun(id)`; "Run again" and Recent runs' "Edit & run" both dismiss the run's card, so Edit & run never lands on a locked form and the dismissal survives navigation (supersedes the "card may return" clause) — cost if wrong: one more store field. Ruling: `outputColumnNames` must not live in the self-registering executor module (UI import would carry the registerExecutor side effect); it becomes the tool definition's pure `outputColumns(prefix, params)` in toolRegistry.ts, consumed by both the executor and the form — cost if wrong: a wider refactor than the finding asked. Accepted as-is: aria-live on the phase line only (a 250 ms ticker in a live region is noise); no browser re-check this round (Task 14 smoke covers it).
Task 11: fix round 1/5 implemented incl. addendum (67042c6..eb241cf); Codex re-review dispatched over review-997c869..eb241cf.diff
Task 12: fix round 1/5 dispatched (fresh Opus implementer; FIX_BASE eb241cf)
Task 11: fix round 1/5 (8 addressed, 1 open — (3) dismissal: Edit & run dismisses only the clicked run while the form follows the LATEST run of the tool/target, and a dismissed id suppresses in-flight state too; new Important breakage: Edit & run on a queued/running run hides its progress and Cancel; commits 997c869..eb241cf)
Ruling (round 2 mechanism): a dismissed id only suppresses a DONE card — ToolView renders idle only when `latestRun.status === "done"` and its id is dismissed; queued/running/cancelling always render their footer and lock the form. Edit & run writes the draft, then dismisses the LATEST run of that (tool, target) pair when it is done (store helper, e.g. `dismissDoneRun(toolId, targetLayerId)` or dismissRun on the resolved latest id); on an in-flight latest run it dismisses nothing (the form opens locked, spec §6.1) — cost if wrong: a card the user meant to keep disappears on Edit & run.
Task 11: fix round 2/5 pending the Task 12 implementer (serialised commits).
Task 12: fix round 1/5 implemented (c94022f..73eb8d6); Codex re-review dispatched over review-eb241cf..73eb8d6.diff; Task 11 round 2 resumes the implementer (FIX_BASE 73eb8d6)
Task 12: fix round 1/5 (2 addressed, 1 residual — the COMPUTED heading uses var(--font-mono), which brand.css maps to the UI family; commits eb241cf..73eb8d6; no new breakage)
Task 12: parked — "the Details COMPUTED heading still requires an actual monospace font" — Ruling: the code stands. brand.css deliberately defines `--font-mono: var(--font-ui)` ("one family across headings, controls and data") and CLAUDE.md makes the brand tokens the only source; the spec's "mono sub-heading" is the brand's mono-label STYLE (uppercase, tracked, `--font-mono`), exactly what the catalogue's group headings (§5 "mono labels") already render. A literal monospace here would be the one element outside the brand's family — cost if wrong: one token change in app.css.
Task 12: complete (commits 2df6d33..73eb8d6 incl. e3cf64f, review clean after 1 fix round, 1 parked with ruling; streaming Details/rules gap deferred to M2 by ruling). Push deferred until Task 11 round 2 lands (the pre-push hook runs the full suite against a working tree an implementer is editing).
Task 11: fix round 2/5 implemented (1b97e0f..e658bf2); Codex re-review dispatched over review-73eb8d6..e658bf2.diff
Task 13: dispatched (Opus; BASE e658bf2) in parallel with the read-only Task 11 round-2 re-review. Ruling: "next palette colour" — the app has no rule-colour palette sequence (NEW_RULE_COLOR_HEX is the editor's one default), so the draft takes the editor's default new-rule colour; a palette rotation is M2 work — cost if wrong: successive drafts share a colour. Ruling: "All values are empty" gates on summary.measured === 0 (the plan's synchronous condition; for Height from extent measured = features that got a value) — cost if wrong: none for M1's tool. Push of Tasks 11+12 deferred to after Task 13 lands (hook runs the suite on the working tree).
Task 11: fix round 2/5 (2 addressed, 0 open; commits 73eb8d6..e658bf2; re-review clean, no new breakage)
Task 11: minor (deferred): eligibility recomputed per candidate layer per render; elapsed ticker outside the live region (accepted); `measure-solids` registry mock in useToolForm.test.tsx to delete when M2 registers the tool; dismissedRunIds capped at 20 with the history; no browser re-verification across the fix rounds (Task 14 smoke covers the tool view).
Task 11: complete (commits 63eb06e..e658bf2 incl. 2df6d33 base + 2 fix rounds, review clean)
Task 13: implemented (fd13eda), DONE_WITH_CONCERNS. Ruling: `colorBy: "rules"` is set eagerly on Style by result — spec §6.2's letter ("opens the target's STYLE section with Color by = Rules"); the map repainting to saved rules / the unmatched colour is the app's existing behaviour of that mode, and "the map does NOT change until Save" governs the DRAFT rule — cost if wrong: a repaint before Save on layers that were on surface/single. Accepted: implementer's one commit ran with `-c core.hooksPath=.vite-hooks` — verified equivalent (`.vite-hooks/*/h`only forwards to`.vite-hooks/<hook>`; `vp staged`ran; no commit-msg hook exists), no hook bypassed. Note for Task 14: Playwright MCP has no Chromium; use agent-browser with the Playwright Chromium at ~/.cache/ms-playwright/chromium-1234 (launch with --remote-debugging-port,`agent-browser connect`).
Task 13: review dispatched to Codex over review-e658bf2..fd13eda.diff
Task 13: review (Codex) Needs fixes — Important: (1) a failed/NULL median silently becomes a `> 0` draft; (2) the target layer is not re-validated after the await; (3) double clicks race and stale completions overwrite the draft. Ruling on (1): a failed median opens NO draft and does not navigate; it raises the store notice with the DuckDB error's first line (formatDuckDBError) — the toast is the existing failure surface; a NULL median (nothing to measure) raises "All values are empty" the same way — cost if wrong: a toast where a draft with an empty value might have served. Minors folded: repeated-toast test advances timers; the disabled reason rendered visibly as a muted note (the Run button's pattern); vp check warning count shown equal at base and head.
Task 13: fix round 1/5 dispatched (resume implementer; FIX_BASE fd13eda)
Task 13: fix round 1/5 implemented (1e4caf7); Codex re-review dispatched over review-fd13eda..1e4caf7.diff
Task 13: fix round 1/5 (4 addressed, 2 open — (1) notice may carry a multi-line formatted error, not the first line; (3) Run again during the await leaves the stale completion valid; new Important: same lifecycle gap; minor: "The query failed." fallback for a missing table is not spec copy; commits fd13eda..1e4caf7)
Ruling: the notice is the FIRST line of formatDuckDBError(message) (spec §6.3 "the first error line"). Ruling: Style by result is disabled with the spec's "stale: layer reloaded" while run.stale; a table that is missing/not ready for a non-stale done run is an impossible state and takes the same silent-abandon exit as a removed layer, so no invented copy remains — cost if wrong: a silent no-op in a state that should not occur.
Task 13: fix round 2/5 dispatched (resume implementer; FIX_BASE 1e4caf7)
Task 13: fix round 2/5 implemented (65fba43); Codex re-review dispatched over review-1e4caf7..65fba43.diff
Task 13: fix round 2/5 (1 addressed, 1 open + 1 minor open — (1) the notice's "first line" split runs on an already-formatted message; minor: measured === 0 outranks run.stale for the disabled title; commits 1e4caf7..65fba43)
Ruling on (1): formatDuckDBError IS the app's "DuckDB's own first error line" (its doc comment; it drops the LINE echo and caret and keeps DuckDB's message, candidate bindings included) and the export dialog shows exactly that, which is what spec §6.3 points at ("as the export dialog shows DuckDB errors"). The notice is therefore the outcome's message verbatim; the extra firstErrorLine split is removed, and the test feeds a raw multi-line DuckDB error through the real formatter — cost if wrong: candidate-binding hints appear in the toast.
Task 13: fix round 3/5 dispatched (resume implementer; FIX_BASE 65fba43)
Task 13: fix round 3/5 implemented (902b1e2); Codex re-review dispatched over review-65fba43..902b1e2.diff
Task 13: fix round 3/5 (2 addressed, 0 open; commits 65fba43..902b1e2; re-review clean)
Task 13: minor (deferred → final review): colorBy "rules" set eagerly repaints saved rules before Save (ruled spec-literal); no browser check in any round (Task 14 smoke covers Style by result → editor → Save recolour).
Task 13: complete (commits e658bf2..902b1e2, review clean after 3 fix rounds)
Push: develop 997c869..902b1e2 (Tasks 11 fix rounds, 12, 13) — dispatched
Task 14: dispatched (Opus implementer: verification, 3 DuckDB probes, smoke, docs; BASE 902b1e2). Codex milestone review pass 'src' dispatched over m1-diff-src.diff at 902b1e2 (tests pass follows Task 14).
Codex milestone review, pass src (m1-review-src.md): 6 MAJOR, 5 MINOR, verdict "Not yet". Adjudication:

- M1 cancel during the write commits: ADDRESS — writeComputedColumns takes the run's AbortSignal and, when aborted before COMMIT, issues ROLLBACK and reports cancelled (nothing changed); an abort after COMMIT keeps §6.1's "finished before the cancel arrived". Ruling: no engine-level cancel (cancelSent is best-effort and out of M1) — cost if wrong: a cancel that lands mid-UPDATE still waits for the statement.
- M2 removing the target mid-run does not cancel it ("Layer removed", §6.1): ADDRESS — runQueue subscribes once to useLayerStore (installer pattern like installRuleDraftInvariants) and aborts queued/running runs whose target left, patching them failed with "Layer removed".
- M3 Matching ids resolved at the head: PARKED — the Task 9 ruling stands (scope INPUTS frozen at Run, resolved at the head; a queued earlier write can only move a filter that references the column it writes) — cost if wrong: an edge case the human can reopen.
- M4 Retry re-snapshots the scope (§6.3 "same frozen parameters"): ADDRESS — runQueue keeps each run's frozen snapshot (module map or on the record) and exports retryRun(runId) that resubmits with it; RunFooter Retry and RecentRuns Retry/Re-run call retryRun; requestFromRun stays for Edit & run's draft.
- M5 case-sensitive collision check while DuckDB identifiers are case-insensitive, and no pre-write revalidation: ADDRESS — compare lower-cased names in useToolForm; the queue re-checks source collisions at the head against the table's current columns (fails with the §6 prefix message) — closes Task 11's deferred "§6.1 re-validation of a column that now belongs to the file".
- M6 parts receive the feature's extent: ADDRESS — spec §8 "a Building shows the aggregated value, a part its own": rollUpExtents writes the roll-up to the root row and each part's own extent to its row (a part with no bbox: NULL).
- m7 write step logs sql: null: DEFERRED (roadmap) — the log would need writeComputedColumns to return its statements; m8 collapsed pills do not set the tab: ADDRESS (cheap); m9 Open table does not scroll the new columns into view: DEFERRED (roadmap); m10 OUTPUT omits "Write to · This layer (<name>)": ADDRESS — render the destination as the sole, checked, disabled radio "This layer (<target>)" under "Write to" (New layer is M2+); m11 "Cancel run" → spec's "Cancel": ADDRESS.
  Fix wave (Codex MAJORs) queued behind the Task 14 implementer; then Codex pass 'tests' over the final diff; then the final whole-branch review + one fix wave.
  Session restart (2026-09-11, evening): Task 14 implementer interrupted after committing the probes (8ef02cd); no report yet; Chromium on 9333 and the dev server on 5173 still up; resumed from its transcript. Ruling unchanged: no commit trailers (the harness's Claude-Session line included).
  Task 14 (implementer part): complete (commits 8ef02cd..95256ec): probes 1–3 accepted (ALTER+backup+UPDATE in one transaction OK; all-NULL infers JSON and still lands in DOUBLE; BigInt-as-string casts with precision loss; mid-transaction reads OK); verification green; smoke 7/9 with 2 deviations. Rulings on its concerns: (a) unimplemented tools read "Not available yet" even when an eligibility reason also applies — stands for 13.1 (an eligibility reason on a tool that cannot run would promise something adding a layer cannot deliver); scenario 1's cross-layer wording is met when those tools ship — cost if wrong: scenario 1 not literal in 13.1; (b) the Style by result draft needs a name before Save — ADDRESS in the fix wave: prefill the draft's name with the attribute name; (c) the collapse chevron is disabled without a selection so the "Tools" pill (§4.2) is unreachable — ADDRESS in the fix wave: the chevron is enabled while the toolbox is open; (d) Delft secondary check skipped (Chromium crashed) — accepted; (e) node_modules lacked @fontsource/source-sans-3 (npm install, lockfiles untouched) — environment note.
  Fix wave 1 (Codex MAJORs M1,M2,M4,M5,M6 + minors m8,m10,m11 + (b),(c)): dispatched to a fresh Opus implementer; BASE 95256ec. Codex pass 'tests' dispatched over tests/, scripts/smoke/, docs/ at 95256ec.
  Codex milestone review, pass tests (m1-review-tests.md): 4 MAJOR coverage gaps + 1 MINOR, "Not yet". Adjudication: T1 hollow Undo-takeover race test (runQueue.test.ts:645) — ADDRESS in fix wave 1; T2 part-extent test cannot detect M6 — covered by M6's red test, made explicit; T3 "frozen request" Retry test mocks submission — covered by M4's test, made explicit (queue-level test that changes the selection before Retry); T4 cancel coverage gates at COMMIT — covered by M1's test, made explicit (gate the UPDATE, cancel, assert ROLLBACK/no COMMIT/unchanged table, model, provenance); T5 ToolView error test replicates the formatter — ADDRESS: use the production formatter via vi.importActual. All forwarded to the fix-wave implementer.
  Cross-session note: the original commander session (multiroof-viewer-a9) stood down after the restart; this session owns the branch from fix wave 1 onward. Its Task 14 implementer added an Addendum to task-14-report.md (probe outcomes + independent smoke reproduction).
  Double-commander notice (original session multiroof-viewer-a9, 2026-09-11 evening): this session was NOT interrupted; the "Session restart" above is a second session (multiroof-viewer-eb) resumed from its transcript while the Task 14 implementer was still running. The original implementer finished Part A + probes (8ef02cd) and appended an Addendum to task-14-report.md. Ruling: the original session stands down from all dispatching and writes from this line on; multiroof-viewer-eb owns fix wave 1 → its re-review → final whole-branch review + one fix wave → push → M2 plan — cost if wrong: none to the tree; one idle commander.
  Fix wave 1: implemented (55ec9fa..5668b08, 13 commits: M1+T4, M2, M4+T3, M5, M6+T2, m8, m10, m11, S1, S2, T1, T5), DONE_WITH_CONCERNS; suite 2817 passing. Adjudication of its concerns: (1) requestFromRun now dead → removed in a closing commit; (2) the provenance registry and mergeAttributes key columns case-sensitively, so a mixed-case replace leaves a stale duplicate entry — minor (deferred → roadmap open items); (3) no statement-level cancel (a cancel waits for the running UPDATE) — accepted, already ruled; (4) the removal watcher covers the target only — accepted (no M1 tool has a source; M3 adds sources); (5) a rebuilt table carries computed values from the model while the registry is cleared, so the prefix then reads as source-owned — recorded as a known issue (roadmap); (6) the wave's UI changes get a short browser re-check appended to scripts/smoke/processing-m1.md before the re-review.
  Fix wave 1: closed (c92e751 removes requestFromRun; 2a8fa2f post-wave browser re-check 5/5); Codex scoped re-review dispatched over review-95256ec..2a8fa2f.diff
  Final whole-branch review dispatched to Codex gpt-6-astra over 0d787fb..2a8fa2f (final-diff-src.diff + final-diff-tests.diff), in parallel with the fix-wave re-review.
  Note for the final fix wave: docs/roadmap.md's 'Carried to 13.2 / M2' list predates fix wave 1 — remove the items it fixed (draft rule name prefilled; §6.1 source-column re-validation at the head; collapse chevron enabled with the toolbox open) and add the new known items (case-sensitive registry/model keying after a mixed-case replace; a rebuilt table's model-carried computed values read as source-owned; cancel waits for the running statement; write-step SQL absent from the log; Open table does not scroll new columns into view).
  Fix wave 1: re-review (Codex) — 17 items ADDRESSED, M2 residual open: after the watcher marks a run "Layer removed", the post-COMMIT continuation (refreshLayerTableColumns resolving) patches it back to done and publishes a notice; and a scope-query failure can overwrite "Layer removed" (runQueue.ts:724 vs :578–590, :390–400). Ruling: real and load-bearing (a removed layer's run must not read done); it is carried into the final review's ONE fix dispatch (the process allows one fix wave after the final review) instead of a second wave now — the fix: execute() re-checks the run's terminal state (or the controller's abort reason) after every await and before every patch to done/failed, and drops the undo resources of a run that ended "Layer removed" — cost if wrong: the defect ships for the minutes until that dispatch.
  Push: develop 902b1e2..2a8fa2f (Task 14 + fix wave 1) — dispatched.
  Advisor (post fix wave): the residual fix is ONE guard — patch() refuses to move a run out of a terminal state — plus discardUndo and no notice for a 'Layer removed' run; the final fix dispatch also carries the roadmap refresh and a committed handoff snapshot (workspace deletion without a snapshot would lose the rulings). M2 planner: Opus, writes only the plan file, no commit; Codex reviews the plan before I commit it. ListAgents run: peer multiroof-viewer-a8 busy, asked to confirm it is not writing to develop.
  Peer multiroof-viewer-a8 confirmed read-only (security review); branch free. M2 planner dispatched (Opus; writes docs/superpowers/plans/2026-09-11-processing-toolbox-m2.md only, no commit).
  Final whole-branch review (Codex, final-review.md): 0 Critical, 5 Important, 2 Minor, "With fixes". Adjudication: I1 mixed-case replacement leaves model/registry/Undo ownership keyed by spelling → ADDRESS (canonicalise output names to the table's existing spelling before write/merge/register; Undo ownership compared case-insensitively); I2 removal residual → ADDRESS (terminal-state guard in patch + discardUndo + no notice); I3 CityParquet export lacks computed columns (§8) → ADDRESS with a stop condition (join the layer table's attributes to the reader by id; if > ~150 lines or a design question, stop and it moves to the M2 plan); I4 runtime engine-death recovery (§6.1 "Analytics engine stopped", table rebuild, Undo invalidation) → DEFERRED to the M2 plan as its own design task (touches the ONE-writer rule and retryEngine; M1's plan never included it) — Ruling cost if wrong: a dead engine mid-run fails runs with the generic engine message and earlier Undos fail at the table; I5 a failed run has no visible way to submit an edited request → ADDRESS: Edit & run dismisses finished (done OR failed) runs, and editing any field while the latest run's failed card shows dismisses that card (Run reappears; Retry stays reachable from Recent runs) — cost if wrong: the failed card leaves on the first keystroke; m1 stale/undone run during a pending median → deferred minor; m2 unseen-failure dot not raised when Tools is not visible → ADDRESS (cheap). Roadmap refresh folded in. Final fix dispatch (the ONE allowed) → fresh Opus implementer, BASE 2a8fa2f.
  Final fix wave: implemented (1622b78..11f2b32, 8 commits: F1, F2, F5, F6, F3 + 2 probes/tests, F7 roadmap), DONE_WITH_CONCERNS; suite 2834; integration 31/31. Concerns: (1) F3's red was reconstructed after the fact (reported honestly) — accepted; (2) F5's draft edit dismisses the failed card of the pair switched TO on a target change — to the re-review; (3) F3's LEFT JOIN assumes reader/table id agreement (pre-existing) — accepted; (4) redundant failedAlready/discardUndo after F2's guard — minor (deferred). Codex scoped re-review dispatched over review-2a8fa2f..11f2b32.diff.
  Final fix wave: re-review (Codex) — F1, F2, F3, F5, F6, F7 ADDRESSED; F5 target-switch concern ACCEPTABLE; no new breakage; no open findings (commits 2a8fa2f..11f2b32). Minor (deferred): F3's RED reconstructed after implementation (disclosed); redundant failedAlready/discardUndo after F2's guard.
  M1 (M13.1): final whole-branch review clean after one fix wave. Remaining: M2 plan (planner running) → Codex plan review → commit plan → handoff snapshot (docs) → push.
  Push attempt after the final wave refused by the pre-push hook: vp check flags formatting in the in-progress M2 plan file only; push repeats after the plan is committed via the pre-commit formatter (no bypass).
  M2 plan review (Codex, m2-plan-review.md): 12 MAJOR, 4 MINOR, "Fix first". Adjudication: fix in the plan — (1) contributor rule keyed on geometry presence at the LoD per §7, not roof-surface presence; (3) Style by result gated on non-null values of the first written column (summary carries the count), not measured > 0; (4) complete form-test fixtures, correct the placeholder-scan claim; (5) subscription tests through a package-level fake under the real duckdb.ts, real transitions, unsubscribe proven; (6) fixture updates for the widened resident type in the same task as the pointer bump; (7) the JSX semicolon + missing import; (8) LoD counts from tags only, measurement bounded to scoped contributors at the chosen LoD in batches with cancellation checks; (10) hide the LoD control on unimplemented tools; (11) offline smoke labelled honestly (cold reload unmet); (12) executor lifecycle behaviour tests; (13)–(16) minors incl. corrected citations and the snapshot rationale. Ruling on (2): the FCB Details/rules write-back stays DEFERRED (the user's M2 scope is Roof metrics + lazy loading + subscription); the plan must state the deferral explicitly as a design note and open question 10 for the human, not as an "unchanged seam" — cost if wrong: the streaming gap persists one more milestone. Ruling on (9): engine-death recovery (§6.1) stays OUT of M2's tasks; the plan carries a "Deferred with design notes" section sketching the mechanism (worker death detection in duckdb.ts → status failed → queue fails running/queued runs with "Analytics engine stopped" → retryEngine rebuilds tables → Undo backups invalidated with "Unavailable after an engine restart") and open question 11 asks the human whether to pull it into M2 — cost if wrong: a dead engine mid-run stays unhandled through M2. Planner resumed for one amendment round; Codex re-review follows.
  M2 plan re-review round 1 (Codex, m2-plan-rereview.md): 9 of 16 addressed/deferral stated; open: 1 (LoD counts still include excluded root roofs), 4, 5, 7 (expression semicolons at 3 sites), 12, 15, 16; 5 new MAJOR (Task 5 overwrites an existing submodule test suite and misses producers; shared fixture inconsistent + missing ColumnInfo.kind; Tasks 10–12 test scaffolding incomplete; Task 9 invents renderDoneRun; lifecycle examples cannot establish their claims) + 2 MINOR. Ruling: the plan fix loop is capped at 2 amendment rounds (this is round 2); whatever remains open after the round-2 re-review is written into the plan under "Review residuals (to resolve at pre-flight)" and into the ledger, and the plan is committed as the M2 starting point — cost if wrong: M2's SDD pre-flight inherits a known list instead of a clean plan.
  M2 plan re-review round 2 (Codex, m2-plan-rereview2.md): 9 addressed, 6 open (4, 5, 12, 15, 16, N5, N6 — test scaffolding/fixture arithmetic/explanatory claims), 3 new MAJOR (chip suite shares a loaded singleton; lifecycle fixture geometry contradicts assertions; lifecycle scaffolding incomplete) + 1 MINOR. Cap reached (ruling above): the residuals are written into the plan as "Review residuals (resolve at M2 pre-flight, before Task 1)" and the plan is committed as M2's starting point. The design decisions, task decomposition, interfaces and all production-code steps were verdicted addressed; what remains is test-scaffolding precision in Tasks 1, 3, 8, 10 — cost if wrong: M2's Task 1/3/8/10 implementers spend a round fixing tests the plan mis-specified.
  Plan committed; handoff snapshot refreshed and committed; push follows.
