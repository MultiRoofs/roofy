# SDD ledger — plan: docs/superpowers/plans/2026-09-10-processing-toolbox-m1.md

Spec: docs/superpowers/specs/2026-09-10-processing-toolbox-design.md (read).
Branch: develop (CLAUDE.md: commit directly on develop, no feature branch).
Milestone start SHA: 0d787fb (before plan commits); plan committed at c4dd549.
Submodule pin: e681c18, detached HEAD.

Ruling: work on `develop` directly, no worktree — CLAUDE.md mandates committing on develop and the user chose this workflow; cost if wrong: an unfinished milestone sits on the shared branch (unpushed until the milestone gate).
Ruling: implementers run on Opus (user instruction: "use Opus as implementor"); task reviewers run on Sonnet; the milestone review is Codex gpt-6-astra (user instruction) in addition to the skill's final whole-branch review — cost if wrong: higher token cost per task.

## Pre-flight scan

| Tasks          | Shared surface                                                                                                                                          | Produces vs consumes                                                                                                                | Finding / ruling                                                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1 → 2,3,4,9,11 | `types.ts` (`RunRecord`, `ToolId`, `Scope`)                                                                                                             | Task 2 store, Task 3 button and Task 9 queue use `RunRecord` fields `status`, `undoable`, `stale`, `note`, `phase`                  | consistent (all fields declared in Task 1)                                                                                               |
| 2 → 4,9,11,13  | `processingStore` (`open`, `view`, `drafts`, `runs`, `notice/noticeSeq`, `upsertRun`, `patchRun`, `pushNotice`, `runById`)                              | Task 9 uses `patchRun`/`upsertRun`/`runById`; Task 13 subscribes to `noticeSeq`; Task 4 adds a shell-store side effect in `setOpen` | consistent; Task 4 step 6 amends Task 2's `setOpen` (documented)                                                                         |
| 3 → 4          | `App.tsx` map header                                                                                                                                    | Task 3 inserts `ToolsButton`; Task 4 edits the right panel props                                                                    | different regions of App.tsx; sequential, no conflict                                                                                    |
| 4 → 11         | `ToolView.tsx`, `LogView.tsx`, `RecentRuns.tsx` placeholders                                                                                            | Task 4 creates placeholders; Task 11 replaces them                                                                                  | intended                                                                                                                                 |
| 5 → 10         | flat rows `bbox` STRUCT                                                                                                                                 | Task 10 SQL reads `"bbox"."zmin"`                                                                                                   | field names `xmin…zmax` match the reader's; ruling: fallback bbox is the object's OWN extent, roll-up covers parts (spec §7.4 corrected) |
| 6 → 9          | `runOnTableQueue`                                                                                                                                       | Task 9 wraps `execute` and `undo` in it                                                                                             | consistent                                                                                                                               |
| 7 → 9,12       | `mergeAttributes`, `setModel`                                                                                                                           | Task 9 calls `mergeAttributes` with `undefined` to delete on undo; Task 7 test covers undefined-deletes                             | consistent                                                                                                                               |
| 8 → 9,11,12    | `writeComputedColumns`, `undoComputedColumns`, `useComputedColumnStore`, `computedColumnsOf`, `provenanceOf`, `formatProvenance` (Task 12 adds)         | Task 9 uses write/undo/store; Task 11 form uses `computedColumnsOf`; Task 12 adds `formatProvenance`                                | consistent                                                                                                                               |
| 9 → 10,11      | `ToolContext`, `ToolResult`, `ToolExecutor`, `registerExecutor`, `submitRun`, `cancelRun`, `undoRun`, `refreshLayerTableColumns`, `buildFeatureRowsSql` | Task 10 registers an executor; Task 11 calls submit/cancel/undo                                                                     | consistent; `tools/register.ts` import side effect ruled in Task 10                                                                      |
| 9 (self)       | scope test rows `{id, f}`                                                                                                                               | step 1 text first shows `{ id }` rows then corrects to `{ id, f }`                                                                  | Ruling: the `{ id, f }` form with `buildFeatureRowsSql` is binding (the later paragraph) — cost if wrong: none, test shape only          |
| 11 (self)      | `RunFooter`, `RecentRuns`, `LogView` are specified in prose, not code                                                                                   | plan rubric prefers code                                                                                                            | Ruling: prose is precise enough (labels, actions, states named); implementer follows spec §6 — cost if wrong: a review round             |
| 12 → 8         | `ComputedAttributeBadge` gains `title` prop                                                                                                             | existing callers keep the default                                                                                                   | consistent                                                                                                                               |
| 13 → 2, 11     | toast via `noticeSeq`; Style by result draft                                                                                                            | Task 11's RunFooter defers the draft to Task 13                                                                                     | consistent                                                                                                                               |
| Global         | new `duckdb.ts` exports                                                                                                                                 | none planned in M1                                                                                                                  | consistent                                                                                                                               |

## Task log

Task 1: implemented (commit 14701ca), DONE*WITH_CONCERNS: pre-existing failures on develop block pre-push — appCityParquetLayers.test.tsx (drop-zone testid) and vp check error DrawOverlay.tsx:130; vitest must run under mise Node 24 shims.
Ruling: commit trailers — none (session instruction overrides CLAUDE.md's Co-Authored-By line) — cost if wrong: trailers absent from history.
Ruling: the two pre-existing develop failures are fixed as a separate chore commit at the milestone gate (Task 14) so pre-push passes; not folded into feature tasks — cost if wrong: a slightly larger gate.
Task 1: complete (commits c4dd549..14701ca, review clean)
Task 2: implemented (commit 12a602e); note: noUncheckedIndexedAccess means later briefs' verbatim tests may need ?. on array index
Task 2: review Approved with 2 Important plan-mandated findings: (a) openTool/openLog open the panel without clearing unseenFailure; (b) patchRun raises unseenFailure for an unknown run id. Ruling: both real; carried into Task 4's dispatch (Task 4 already amends processingStore.setOpen) instead of a parallel fix dispatch while Task 3 runs — cost if wrong: Task 4 grows by two small edits + tests.
Task 2: minor (deferred): no tests for back() from a log view, openLog while closed; resetForTest has no repo precedent.
Task 2: complete (commits 14701ca..12a602e, review approved; 2 findings carried to Task 4)
Task 3: implemented (commit 2d55992); browser-checked via Playwright; peers 32px vs 30px pre-existing drift noted
Task 3: complete (commits 12a602e..2d55992, review clean; minor: brief listed flatControls.css unused)
Task 4: implemented (commits d6ebf06, 7b128d2); agent stalled after writing the report (watchdog) — treated as DONE
Ruling (user, 2026-09-11): pushing develop is authorised; the pre-existing pre-push blockers (appCityParquetLayers.test.tsx drop-zone testid; DrawOverlay.tsx:130 no-unused-expressions) are fixed as a chore commit right after Task 5 (sequenced, never parallel with an implementer); push after that and after each reviewed task from then on.
Task 4: review Approved; Important: ToolsButton closes the toolbox when the Details tab is showing instead of switching to Tools (spec §4.1). Ruling: real; fix carried into Task 11's dispatch (lift `tab` into processingStore as `activeTab` + `setTab`; ToolsButton switches/expands and only closes when Tools is visibly active; openTool/openLog also un-collapse the panel) — cost if wrong: Task 11 grows; a tool form can be lost to the button until then.
Task 4: minor (deferred → Task 11): rename the misnamed Escape test (closed toolbox on a tool view clears the selection) and add sheet-before-form coverage; remove dead `--font-label` (use --font-mono) and unused `data-extension`/`.processing-catalogue` hooks or give them consumers; tab widget a11y (aria-controls/tabpanel) and `aria-disabled` on clickable rows revisited with the tool form.
Task 4: complete (commits 2d55992..7b128d2, review approved; 1 Important carried to Task 11)
Task 5: implemented (commits 8bfd3c1, eb9a5b4); also added FLAT_COLUMN_TYPES.bbox in layerTables.ts (ALTER loop needs a type); integration suite 20/20
Task 5: minor (deferred): make FLAT_PREFIX_COLUMNS a const tuple keyed into FLAT_COLUMN_TYPES so a missing type is a tsc error; mixed-null bbox inference untested; no parsed-model→row bbox test.
Task 5: complete (commits 7b128d2..eb9a5b4, review approved)
Chore: 0d035ea fixes the two pre-push blockers; pushed develop 0d787fb..0d035ea (spec, plan, mockup, Tasks 1–5)
Task 6: first implementer stalled in the full-suite run (watchdog); the pending 40-line diff (runOnTableQueue + 2 tests) was read in full by the controller and matches the brief exactly; a fresh implementer commits/pushes it. Ruling: controller review suffices for this diff (read completely; FIFO ordering and rejection tests assert behaviour) — cost if wrong: an unreviewed 6-line function.
Note: index.html carries an uncommitted launch-screen tagline edit made outside this run (not by any agent); left untouched, surfaced to the user.
Task 6: complete (commit af6534e, controller-reviewed); pushed 0d035ea..af6534e (index.html WIP stashed around the push and restored)
Task 7: implemented (submodule a890c63 → origin/main; parent a4c1218); pushed af6534e..a4c1218 with the hook's suite green (2661) — implementer's 38 'Node-24 localStorage' failures were an env artefact
Task 7: minor (deferred): handleSync.test.ts:307 asserts setModel arg by deep equality (use toBe); repaint() lacks a null-evaluator early-out (plugin; add `if (!this.evaluator) return` — safe); mergeAttributes re-mints a model for no-op patches; `as unknown as CityModel` cast in the plugin fixture.
Task 7: complete (commits af6534e..a4c1218 + plugin e681c18..a890c63, review approved)
Task 8: implemented (commit d673991); pushed a4c1218..d673991. Concerns carried to Task 9: empty rows must short-circuit before writeComputedColumns; read_json_auto inference on all-NULL columns to probe at the milestone smoke
Task 8: review Approved with 4 Important plan-inherited items. Rulings: (1) empty-rows guard lives in Task 9 (already in its dispatch); (2)(3) read_json_auto inference (all-NULL / BigInt-as-string / sample_size) and ALTER-then-UPDATE-in-one-transaction are PROBES against real DuckDB 1.5.5 in the Task 14 gate using the DUCKDB_INTEGRATION=1 harness; if the transaction order is refused, split into ALTER outside + UPDATE inside and document; (4) the shared connection has no other BEGIN user (grep-confirmed) and runs are serialised on the table FIFO; concurrent reads inside the transaction are harmless — accepted for M1, noted in architecture-notes at the gate — cost if wrong: a probe failure at the gate forces a rework of writeComputedColumns.
Task 8: minor (deferred): `{ ...row, id }` ordering so an output column named id cannot clobber the key; runtime whitelist of ColumnType; full-order assertion in the write test; removeColumns on an untouched layer mints an empty entry; computedColumnsOf returns a fresh Set (UI must not use it as a selector snapshot).
Task 8: complete (commits a4c1218..d673991, review approved)
Task 9: implemented (b8d9f85, dc088cd, 17a6b9f); implementer concern: scope inputs read at queue head not frozen at Run (spec §6.1) → fix round after review
Task 10: implemented (66d5f89, 368de10); pushed
Task 9: review Needs fixes — Important: (1) undoRun's refreshLayerTableColumns runs outside the queued task (Undo→Run again reads a dropped column; rebuild race clobbers registry); (2) Provenance.partial counts rows not features; plan-mandated (3) scope resolved at queue head, not frozen at Run. Ruling on (3): freeze the scope INPUTS at Run (selection object ids for `selected`, the applied FilterGroup for `matching`) in submitRun and resolve at the head from that snapshot; a queued run whose target table was rebuilt fails with the §6.1 reason — cost if wrong: ids resolved slightly later than the spec's letter, but the goalposts cannot move.
Task 9: fix round 1/5 dispatched (findings 1–3 + minors: abort check after scope; drop \_\_undo* tables when a run's Undo is taken away or it leaves the history; tests for "finished before the cancel arrived" and the Undo takeover; elapsedMs on early failures).
Task 10: minor (deferred): register.test.ts pins EXECUTORS to exactly ["height-from-extent"] (edit when the next tool registers); zmin>zmax corrupt bbox writes a negative height (decision: honest report, no guard); Task 11 must build RunRequest.columns from the same prefix the executor uses.
Task 10: complete (commits 17a6b9f..368de10, review approved)
Task 9: fix round 1/5 (8 addressed per implementer; commits 0dd69e4, fd8a6f8, 63eb06e); pushed through 63eb06e; external launch-screen WIP (index.html, launchScreen.ts, launchScreen.test.ts) stashed around the push and restored
Task 9: fix round 1/5 (8 addressed, 0 open; commits 368de10..63eb06e; re-review clean, no new breakage)
Task 9: minor (deferred): backup-table cleanup on 21-run eviction untested; matching/selected scopes can now be refused by a failed feature-count query.
Task 9: complete (commits d673991..63eb06e, review clean after 1 fix round)
Task 11: implemented (a1b1e39, bbba686, c9be262, 2df6d33) incl. Task 4 carried items A/B/C; browser-verified end to end; pushed. Concern → Task 12: computed columns must be in the drawer's default visible column set (spec §8)
Task 11: review Needs fixes — Important: (1) failed card's Retry calls the form's run() (dead when invalid; re-runs the draft not the frozen params) → use submitRun(requestFromRun(run)); (2) "Queued behind X" shown before anything is queued → gate on latestRun.status === "queued"; (3) plan-mandated: §6.2 "Run again unlocks the form" — Ruling: follow the spec's letter: the form stays locked while the latest run's done/failed card shows; Retry re-runs the frozen request; Run again unlocks the form (returns the footer to idle with the same values) without submitting — cost if wrong: one extra click to re-run; (4) OUTPUT_COLUMNS duplicated from the executor with no equality test → export the name builder from the tool module and consume it in both; (5) eligibleTargets filters on table-ready only, not tool eligibility → filter with toolEligibility per layer. Minors 6–9 folded (matching positive test; duplicate .processing-back rules + 30px magic number; queued footer reason precedence; aria-live on the progress block, warnings keyed by index). Fix round 1 dispatches after Task 12 commits (no parallel implementers).
Task 11: minor (deferred, cross-task → Task 14 gate): startedAt set at submit vs elapsed measured from execute (live timer jumps back); scopeCount 0 in a queued run's log header; §6.1 re-validation of a column that now belongs to the file is not implemented.
Task 12: implemented (e3cf64f); browser-verified; review NOT yet run (handover)
