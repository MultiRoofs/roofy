You are the milestone reviewer for Roofy's processing toolbox, Milestone 2 (M13.2), on branch develop. This is pass "tests" of a two-pass review (the other pass covers the source diff); report only on what this pass shows.

Read, by path:

1. The spec (authority): /data2/hideba/multiroof-viewer/docs/superpowers/specs/2026-09-10-processing-toolbox-design.md — §5 (chips), §6 (LoD, PARAMETERS, extension note), §6.1 (phases; engine death), §7 common rules, §7.1 Roof metrics, §8, §10 scenarios 4 and 6.
2. The owner's decisions that narrow the spec (binding): the "Decisions recorded" and "Future consideration" sections of /data2/hideba/multiroof-viewer/docs/superpowers/plans/2026-09-11-processing-toolbox-m2.md — engine death = features unavailable, NO recovery; the FCB attribute write-back deferred; the CLAUDE.md ONE-writer rule rewritten; the adapted copy strings; all six measures on; strict threshold.
3. The hard rules: /data2/hideba/multiroof-viewer/CLAUDE.md (Hard Rules section, as rewritten in this milestone).
4. The diff for this pass: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-11-processing-toolbox-m2/m2-diff-tests.diff (git diff 0519873..HEAD over tests/, scripts/smoke/ and docs/). It is large; read it in sequential chunks until the end. Do not run git commands; read-only on this checkout; inspect files outside the diff only for a concrete named risk (one focused check per risk, named). Do not dispatch subagents.
5. The M2 ledger of rulings, parked findings and deferred minors: /data2/hideba/multiroof-viewer/.superpowers/sdd/2026-09-11-processing-toolbox-m2/progress.md (lines containing "Ruling", "parked", "minor (deferred)"). A ruling is a decision the human will read; do not re-litigate it, but flag it if the code contradicts the ruling or the ruling breaks §7.1 / scenario 4 / a hard rule.

Report correctness bugs, spec deviations (§5, §6, §6.1, §7, §7.1, §8, scenarios 4/6 as narrowed), missing or hollow tests, and hard-rule violations (duckdb.ts the sole importer of @duckdb/duckdb-wasm; ONE publisher of the DuckDB status, React reads only via useDuckDBStatus(); retryEngine() the door; @navaramap imports only in the named engine-binding modules; submodule-first; test files import from "vitest"; every user-visible string verbatim from the spec or the recorded adapted copy). Number findings, tag each CRITICAL / MAJOR / MINOR, give file:line and a concrete fix. Be terse: one line per finding plus the fix. End with a one-line verdict: "Ship" or "Not yet" and why.
