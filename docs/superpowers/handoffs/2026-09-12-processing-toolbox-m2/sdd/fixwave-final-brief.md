# M2 final fix wave — whole-branch review findings

TDD red first per item. Locate edit sites by the quoted code (line numbers drift). Every `vi.mock` of `insights/duckdb` exports what its module under test imports (`onEngineDeath`, `getEngineGeneration`, `subscribeDuckDBStatus`, `getDuckDBStatusVersion`, …). Tests import from "vitest"; `noUncheckedIndexedAccess`; lint baseline 56; `tsc -b --noEmit` clean; commits small, by path, prefixes `fix:`/`test:`/`docs:`, NO trailers; no push; no subagents.

## I1 (Important) — streaming processing depends on the table panel being open

"`src/features/layers/layerTableLifecycle.ts:86`, `src/ui/processing/useToolForm.ts:115`: Streaming commits rebuild tables only while `tablePanelOpen` is true. Tools reads its scope from that table, while its LoD options read current residents. Opening Tools directly can show zero buildings and disable Run; after closing the table and panning, processing can use outdated rows against newer geometry. Result staleness also depends on a table rebuild."
RULING: the streaming-table lifecycle treats the processing toolbox as a table consumer. In `layerTableLifecycle.ts`, the condition that gates (re)building a streaming layer's table on a stream commit becomes `tablePanelOpen || processingOpen || runInFlightFor(layerId)` where `processingOpen` is `useProcessingStore.getState().open` (features → features import is allowed; verify no `ui/` import is needed) and `runInFlightFor` is true while a run targeting that layer is queued/running/cancelling. Opening the toolbox while a streaming layer already has residents triggers the same build the table panel opening does today (find how `tablePanelOpen` becoming true triggers a build and mirror it for `processingStore.open`). Tests (red first): (a) toolbox open, table panel closed, a streaming layer commits → its table is built; (b) a later commit → rebuilt, and an earlier run of that layer reads stale ("stale: layer reloaded" via the M1 watcher); (c) toolbox closed, table panel closed → no build (today's behaviour). Then re-run the scenario-4 streaming steps in the browser WITH THE TABLE PANEL CLOSED (same recipe as `scripts/smoke/processing-m2.md`; `agent-browser set viewport` breaks the canvas — do not use it) and update the smoke record: remove the "prerequisite / not a bug" wording, record the observed results with the date.

## I2 (Important) — Undo and backup cleanup can strand the FIFO on engine death

"`runQueue.ts:426`: `discardUndo()` queues an unprotected `runQuery()`; `runQueue.ts:937`: `undoRun()` awaits `undoComputedColumns()` without a death race. If the worker dies during either, the shared FIFO can remain occupied indefinitely."
REQUIRED: wrap both with `raced(…, null)` from `src/insights/engineAwait.ts` (the death race; no abort signal for these) — `discardUndo`'s DROP and every engine await inside `undoRun` (through `undoComputedColumns`; give it the same signal/death treatment `writeComputedColumns` got: on death, no further SQL, no ROLLBACK against a dead engine, no model/provenance publication; the run's card reads whatever the death watcher set). Regressions: a never-settling DROP inside discardUndo + engine death → the FIFO accepts the next task; a never-settling statement inside undoRun + death → undoRun settles, no model change, the next queued task runs.

## m1 (Minor) — the failure reason is not keyboard-visible

"`CatalogueView.tsx:170`, `processing.css:202`: the description is present but permanently clipped; native `title` is not keyboard-visible." Fix: reveal the description element on `:focus-visible` of the Retry button / row (CSS: un-clip the `.processing-sr-only` sibling via `:focus-visible + …` or a `:focus-within` on the row), keep `aria-describedby`; a test asserting the element is not `aria-hidden` and has the reason text is enough (jsdom has no CSS), plus a one-line note in the smoke record if you verify it in the browser.

## m2 (Minor, docs) — architecture-notes describes the fixed table-build hang as outstanding

"`docs/architecture-notes.md:291` says wiring death races into table builds is future work and only the run queue races death." Fix the paragraph: builds and their cleanup race the death signal (fix wave 1); the remaining unprotected engine callers are the export dialog and the layer counts (`runQuery` outside the queue) and `retryEngine`'s boot wait.
