# SDD ledger — plan: docs/superpowers/plans/2026-09-11-processing-toolbox-m2.md

Spec: docs/superpowers/specs/2026-09-10-processing-toolbox-design.md (read). Branch: develop (commit directly, push after each reviewed task). Milestone start SHA: 0519873 (M1 complete). Submodule pin: a890c63.
Process (user instruction): Fable commander/advisor; Opus implementers, one at a time; Codex gpt-6-astra reviews (task reviews + scoped re-reviews + milestone review); no commit trailers; Node 24 shims for vitest; never bare vite; hooks never bypassed.

## Rulings carried in (the human's answers, 2026-09-11, binding)

Ruling (user): the CLAUDE.md "ONE writer of the DuckDB status" rule is rewritten in M2 Task 1 (duckdb.ts publishes; `useDuckDBStatus()` is the one React door; App's mirror deleted) — cost if wrong: the rule text and hook to revert.
Ruling (user): the FCB attribute write-back (streaming values into Details/rules) is NOT in M2 — "future consideration"; streaming compute stays — cost if wrong: a streaming run's values stay table-only.
Ruling (user): engine death = features unavailable, no recovery: status failed → rows disabled with the existing reason; queued/running runs fail with "Analytics engine stopped"; Undo disabled with "Unavailable: the analytics engine stopped" (adapted copy); the status bar's Retry unchanged (no table rebuild) — cost if wrong: after an engine death the user reloads.
Ruling (user): all proposed copy accepted (LoD noun, empty-LoD text, chip tooltips, extension-failure sentences incl. navigator.onLine advisory, measure labels + hover explanations, resident-set card line, M1's long description); all six measures on; strict `<` threshold, default 5°, 0–15 — cost if wrong: one-line string/default edits.
Ruling: the plan's "Review residuals" section (Codex, 2 rounds) is resolved at pre-flight by the Task 1 implementer's dispatch or by targeted plan edits before each affected task (Tasks 1, 3, 8, 10 in the pre-renumber numbering); each resolution is a ledger line.

## Pre-flight scan (2026-09-11, plan at 8102e78)

| Tasks                                | Shared surface                                                                                                                                                                                                            | Produces vs consumes                                                          | Finding / ruling                                                                                                                                                                                                                         |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 → 3, 4                             | `duckdb.ts` `setStatus`, listener set, `subscribeDuckDBStatus`, `getDuckDBStatusVersion`, `useDuckDBStatus()`                                                                                                             | 3 reads via `useEligibilityContext`; 4 publishes `failed` through `setStatus` | consistent                                                                                                                                                                                                                               |
| 1 → every later test file            | 26 `vi.mock(".../insights/duckdb")` factories gain two keys                                                                                                                                                               | Tasks 3, 9, 10, 11, 12, 13 add NEW test files                                 | Ruling: every new mock factory of `insights/duckdb` must export `subscribeDuckDBStatus` and `getDuckDBStatusVersion` (and whatever the module under test imports) — carried in every dispatch; cost if wrong: a failing import at review |
| 2, 4, 9, 10 → `runQueue.ts`          | phase branch (2), status watcher (4), `throwIfCancelled` (9), `summarise` (10)                                                                                                                                            | sequential edits of one file                                                  | consistent; line references in the plan drift after each — Ruling: implementers locate edit sites by the quoted code, never by the plan's line numbers                                                                                   |
| 4 → 10                               | `processingStore.engineStopped`, `RunFooter` Undo gating                                                                                                                                                                  | 10 edits RunFooter's Style gate                                               | sequential, different regions                                                                                                                                                                                                            |
| 5 → 7, 9                             | `RoofSurfaceMetric`, `rollUpRoofSurfaces(surfaces, flatThresholdDeg) → RoofRollUp \| null`                                                                                                                                | 9 is the only roll-up caller                                                  | consistent                                                                                                                                                                                                                               |
| 6 → 7                                | `ResidentRoofMetrics` (lod), `geometryLods` on resident records (submodule)                                                                                                                                               | 7 reads both                                                                  | consistent; submodule-first commit + pointer bump; existing `objectRecords.test.ts` extended, `streamLayer.test.ts` producer updated                                                                                                     |
| 7 → 9, 11                            | `roofGeometrySource`, `roofLodOptions`, `LodOption { lod, features }`                                                                                                                                                     | 9 measures; 11 lists                                                          | consistent; LoD counts apply the geometry-keyed contributor rule (review finding 1, addressed)                                                                                                                                           |
| 8 → 9, 11, 12, 13                    | `roofParams`, `ROOF_MEASURES`, `FLAT_THRESHOLD_*`, `ToolDefinition.needsLod/validateParams/normaliseParams/outputColumns`, `implemented` flip (13)                                                                        | consumers match the declared names                                            | consistent                                                                                                                                                                                                                               |
| 11 → 12                              | `tests/unit/ui/processing/roofLayerFixture.tsx` (`addRoofLayer`), ToolView fieldset order TARGET → PARAMETERS → OUTPUT                                                                                                    | 12 inserts between                                                            | consistent                                                                                                                                                                                                                               |
| 12 → 13                              | registry shim in the two UI test files removed by 13                                                                                                                                                                      | 13 tests the real registry                                                    | consistent                                                                                                                                                                                                                               |
| Residuals (plan §"Review residuals") | chip suite module isolation (3); lifecycle fixture geometry + scaffolding + a real CPU-batch cancel (9); empty-LoD query scoping (11); false cancel/Retry explanations (1, 9, 14); runnable/verified claims (self-review) | —                                                                             | Ruling: each residual is carried verbatim into the dispatch of the task it names, as a requirement; the reviewer checks it; cost if wrong: a review round                                                                                |
| Global                               | `@duckdb/duckdb-wasm` imported only by `duckdb.ts`; Task 1's package-level fake lives in tests only                                                                                                                       | Tasks 1, 3, 4 mock the PACKAGE under the real module                          | consistent with the hard rule (tests may mock the package)                                                                                                                                                                               |

Task 1: dispatched (Opus; BASE 8102e78)
Task 1: implemented (e4682f8; CLAUDE.md rule rewritten in the same commit), DONE_WITH_CONCERNS. Ruling: concern 1 (a `loading` publish before boot never clears) is a correctness gap → fixed before review (guard the loading write on a live connection, red-first test); concerns 2 (URL stub order; Array.from vs spread for the lint) and 3 (useLayerCounts.test.tsx carve-out) accepted — cost if wrong: none expected.
Task 1: pre-review fix c22cb02; Codex review dispatched over review-8102e78..c22cb02.diff. Task 2: dispatched (Opus; BASE c22cb02) in parallel with the read-only review.
Task 1: review (Codex) Needs fixes — Important: (1) listener exceptions are not isolated (a throwing subscriber aborts boot/extension load) → per-listener try/catch + regression; (2) useLayerCounts.test.tsx mock lacks the two exports — Ruling: the carve-out is withdrawn; the EVERY-factory rule holds (cheap, prevents a later import failure); minors folded: assert the hook's unsubscribe on unmount; fix the copied-Set comment. Fix round 1 resumes the implementer AFTER Task 2's implementer reports (serialised commits).
Task 2: implemented (788f9da), DONE; concerns: offline sentence has no automated test (navigator.onLine is true in jsdom); the extension download runs inside the table FIFO (documented trade); live ticker resets at the extension→compute hand-off (roadmap bullet). Codex review dispatched over review-c22cb02..788f9da.diff.
Task 1: fix round 1/5 dispatched (resume implementer; FIX_BASE 788f9da).
Task 2: review (Codex) Needs fixes — Important: (1) plan-mandated: the live elapsed timer resets at the extension→compute hand-off because compute re-patches startedAt — Ruling: fix now (one execution start timestamp for the whole run; the M1 deferred "timer jumps back" minor closes with it) rather than defer per the brief; (2) no deferred-load coverage (queue blocked during loading; cancel during loading writes nothing; the offline sentence + preserved warning untested) → add deferred-load tests and an offline test (stub navigator.onLine via Object.defineProperty). Minor folded: the skip test asserts no "extension" phase, not just no download. Fix round 1 queued behind Task 1's fix round (serialised commits).
Task 1: fix round 1/5 implemented (cc1e345); Codex re-review dispatched over review-788f9da..cc1e345.diff. Task 2: fix round 1/5 dispatched (resume implementer; FIX_BASE cc1e345).
Task 1: fix round 1/5 (4 addressed, 0 open; commits 788f9da..cc1e345; re-review clean, no new breakage)
Task 1: complete (commits 8102e78..cc1e345 incl. e4682f8, c22cb02; review clean after 1 fix round). Push after Task 2's review (serialised with its fix round).
Task 2: fix round 1/5 implemented (689e75d); Codex re-review dispatched over review-cc1e345..689e75d.diff. Task 2 note: the roadmap's 'timer jumps back' bullet is now stale for the execution half → reword in Task 14. Task 3: dispatched (Opus; BASE 689e75d).
Task 2: fix round 1/5 (3 addressed, 0 open; commits cc1e345..689e75d; re-review clean)
Task 2: minor (deferred): the report's test-count explanation is off by one (four tests added, not three) — cosmetic.
Task 2: complete (commits c22cb02..689e75d incl. 788f9da; review clean after 1 fix round). Push of Tasks 1–2 after Task 3 lands (hook lints the working tree).
Task 3: implemented (75b2221), DONE; concerns: three identical Retry accessible names when spatial fails; no light-theme browser check; row-level download reason unreachable until an extension tool ships. Push develop 0519873..75b2221 (plan + Tasks 1–3). Codex review dispatched over review-689e75d..75b2221.diff. Task 4: dispatched (Opus; BASE 75b2221).
Task 3: review (Codex) Needs fixes — Important: (1) the Retry test waits for the Retry button to vanish (which happens at "loading"), not for completion → wait for the loaded tooltip; (2) the download reason is only in a non-focusable chip's title → expose it through an accessible description (aria-describedby) on the row/Retry and a focus-visible tooltip. Minors folded: give each Retry an extension-specific accessible name ("Retry loading the spatial extension"); scope the expected engine warnings with a console spy. ⚠️ both-themes browser check → resolved: covered by the Task 15 gate smoke (chip states are in its scenario-6 step). Fix round 1 queued behind Task 4's implementer.
Task 4: implemented (5bad30e, b64fd35 — rewritten from fc88aa7/c4e6beb to strip the Claude-Session trailers the implementer added against the ruling; unpushed, local rewrite only), DONE. Rulings on its concerns: (a) terminating the dead worker in the error handlers — accepted (a Retry would otherwise stack a second heap); (b) the watcher reacts only to ready → failed, not to a boot that never came up — accepted, correct; (c) after the status bar's Retry the engine is ready but the layer tables are gone, so rows re-enable and a run fails at its table — accepted as the owner's "no recovery" choice; the docs task records it as the known behaviour ("after an engine death, Retry reboots the engine but does not rebuild tables; reload to continue") — cost if wrong: a misleading re-enabled row until reload. Codex review dispatched over review-75b2221..b64fd35.diff. Task 3: fix round 1/5 dispatched (resume implementer; FIX_BASE b64fd35).
Task 4: review (Codex) Needs fixes — Important: (1) an aborted run stays parked at an engine await that never settles (dead worker), so the table FIFO is occupied forever → race every engine await in execute (scope query, extension load, write steps) against the abort signal (CancelledError on abort) and skip cleanup SQL against a dead engine (writeComputedColumns' ROLLBACK guarded on the engine being ready); never-settling-query test; (2) extension memos survive the dead engine → markEngineDead clears extensionPromises and an engine generation counter stops old continuations from mutating a replacement engine; (3) the watcher latches sawReady → track the immediately preceding state, react to ready → failed only, test ready→failed→initializing→failed; (4) tools re-enable after Retry against tables that died with the engine — Ruling: INVALIDATE, don't rebuild: on engine death every `ready` layer-table entry becomes `failed` with the message "Analytics engine stopped", so rows disable with the spec's own table reason and stay honest after Retry; the implementer reports whether retryEngine then rebuilds those failed entries (if it does, base tables return without computed columns, which is acceptable and documented) — cost if wrong: a row that stays disabled where a rebuild would have worked. Minor folded: init completion guarded against a retired engine (the generation counter). Fix round 1 queued behind Task 3's fix round.
Task 3: fix round 1/5 implemented (78e32aa); Codex re-review dispatched over review-b64fd35..78e32aa.diff. Task 4: fix round 1/5 dispatched (resume implementer; FIX_BASE 78e32aa).
Task 3: fix round 1/5 (4 addressed, 0 open; commits b64fd35..78e32aa; re-review clean)
Task 3: minor (deferred): the accessibility test suppresses console.warn without asserting it (extensionChip.test.tsx ~287–310) — add the same warning assertion; no light-theme browser check of the muted chip (gate smoke).
Task 3: complete (commits 689e75d..78e32aa incl. 75b2221; review clean after 1 fix round). Push after Task 4's fix round.
Task 4: fix round 1/5 implemented (968ecf3, 2a41b3d, bd0b565, bc61764); reported: retryEngine leaves invalidated entries failed, so rows keep the table reason until reload (the owner's "unavailable" shape holds). Concerns → minor (deferred, docs/roadmap): a table BUILD in flight at death still parks the FIFO; runQuery callers outside execute (export dialog, useLayerCounts) await a promise that never settles after a death. Codex re-review dispatched over review-78e32aa..bc61764.diff. Task 5: dispatched (Opus; BASE bc61764) in parallel with the read-only re-review.
Task 4: fix round 1/5 (1 addressed — the watcher transition; 3 open — (1) releasedOnDeath falls back to an unguarded await once Cancel has already aborted, so cancel-then-death strands the FIFO; (2) doInit/ensureExtension assign platform/loadedExtensions before the generation check; (4) queued/active table builds overwrite the invalidation (building → ready; failure paths restore a captured ready entry); new Important = (1)'s mechanism; minor: layerTables' subscriber keeps no unsubscribe for hot reload and resetLayerTablesForTest leaves the tracker; commits 78e32aa..bc61764).
Ruling (round 2): death is an INDEPENDENT signal — duckdb.ts exposes a per-generation "death" promise/subscription (resolves when the engine leaves ready), and every engine await in execute races BOTH the abort signal and that death signal, whatever the cancel state; metadata reads are awaited into locals, generation checked, then assigned; every table build carries the engine generation it was enqueued under and, at start and before each state write, abandons (entry stays failed "Analytics engine stopped") when the generation moved or the engine is not ready, and a build's failure path never restores a captured ready entry after a death; the subscriber's unsubscribe is kept and disposed on hot reload, the tracker reset in resetLayerTablesForTest — cost if wrong: a stuck FIFO or a lying "ready" table after an engine death.
Task 4: fix round 2/5 queued behind Task 5's implementer.
Task 5: implemented (f9ce722), DONE; Ruling: the +1 lint warning (erasing-op in the brief's verbatim test arithmetic) is fixed before review — the 56-warning baseline is a gate; accepted: plan-specified test path; the brief's '9 tests' typo. Task 4 round 2 waits for that one-line commit.
Task 5: lint fix 82b10c2 (baseline 56); Codex review dispatched over review-bc61764..82b10c2.diff. Task 4: fix round 2/5 dispatched (resume implementer; FIX_BASE 82b10c2).
Task 5: review (Codex) Needs fixes — Important: division guards use `areaM2 > 0` where the plan's table says NULL when `area === 0` (a negative or NaN total area yields NULL instead of a number). Ruling: parked — the code stands: spec §6.2's value rule is "NULL means could not be evaluated", and a non-positive or NaN total roof area is exactly that; the table's `=== 0` describes the ordinary case, and Task 9's inputs are geometry-derived non-negative areas — cost if wrong: a NULL where a nonsense number would have appeared. Minor (deferred → final review): explicit single-surface assertions and azimuth at threshold equality incl. a horizontal surface at threshold 0.
Task 5: complete (commits bc61764..82b10c2, 1 parked with ruling)
Task 4: fix round 2/5 implemented (801d080, 480b172, d54c6f9, 75e2da5). Concerns → minor (deferred, docs): a table build whose statement is in flight at death still holds the FIFO (onEngineDeath exists; wiring it into layerTables' build steps is future work); runQuery callers outside execute (export dialog, counts) have the same gap; refreshLayerTableColumns/retryEngine write state without a death check. Codex re-review dispatched over review-82b10c2..75e2da5.diff. Task 6: dispatched (Opus; BASE 75e2da5; submodule-first).
Task 4: fix round 2/5 (2 addressed — (1) death signal, (2) metadata ordering; open: (4) builds carry a separate death count that only advances on ready → failed, start/catch guards omit readiness, and a death during `await discardHalfBuilt` bypasses the sole catch guard so the source is parked or the ready entry restored; the readiness-only branch returns ENGINE_DEAD without writing failed; minor: no hot-dispose of the module-level subscription; commits 82b10c2..75e2da5).
Ruling (round 3): builds carry duckdb.ts's ENGINE GENERATION (one counter, captured at enqueue; a reboot between enqueue and any write also means the tables are gone), and the abandon check (generation moved OR status not ready) runs before EVERY state write in the build — start, after initDuckDB, pre-publish, in the catch both before and after discardHalfBuilt, before parking or restoring — and the abandoning branch itself writes { state: "failed", message: "Analytics engine stopped" } rather than assuming the subscriber did; the subscription registers an `import.meta.hot?.dispose` to unregister on hot reload — cost if wrong: a lying table entry after an engine death during a build's cleanup.
Task 4: fix round 3/5 queued behind Task 6's implementer.
Task 6: implemented (submodule ea8fa64 pushed to origin/main; parent 5fd607f), DONE; a third parent fixture (legendCounts.test.ts) needed the field. Codex review dispatched over review-75e2da5..5fd607f.diff (+ submodule diff appended). Task 4: fix round 3/5 dispatched (resume implementer; FIX_BASE 5fd607f).
Task 6: review (Codex) Approved. Minor (deferred): pin first-seen ordering of geometryLods directly and assert an untagged roof's lod: null; layerRows.test.ts's cast resident fixture lacks geometryLods.
Task 6: complete (submodule a890c63..ea8fa64 pushed; parent 75e2da5..5fd607f; review approved)
Task 4: fix round 3/5 implemented (26d64a8); implementer narrowed the ruling: the abandon check at the head/post-init/catch keys on the generation alone (readiness is applied at the publish), keeping the existing "engine dies at registration → park" path — accepted pending the re-review. Codex re-review dispatched over review-5fd607f..26d64a8.diff. Task 7: dispatched (Opus; BASE 26d64a8).
Task 4: fix round 3/5 (2 addressed — 4b, hot dispose; open: 4a builds enqueued before the engine is ready capture a null generation, so their guards are inert until adoption and a death during boot (or while they are queued behind a dying run) can park the source or revive an invalidated queued build; minor: the catch's abandonment writes before checking superseded(), overwriting a removal; test: the replacement test's DESCRIBE wait can match the first build's SQL; commits 5fd607f..26d64a8).
Ruling (round 4, fresh implementer per the skill's escalation — Opus, the user's implementer tier, with a narrowed mechanism): (i) layerTables' invalidation subscribes to duckdb.ts's engine-death signal (onEngineDeath, added in round 2), not to a status transition, so a death during initialization also invalidates; (ii) the build's head guard abandons when the entry is ALREADY failed with "Analytics engine stopped" (the invalidation is the source of truth for queued builds), in addition to the generation check once bound; (iii) a pre-ready build binds its generation when initDuckDB resolves and, if the engine is not ready at that moment, abandons with the engine-stopped failure rather than parking (parking stays for a boot that never came up before any death); (iv) the catch's abandonment checks superseded() first — removal owns the entry; (v) the test's DESCRIBE needle is made distinct per build — cost if wrong: an invalidated build revived after a boot-time death.
Task 4: fix round 4/5 queued behind Task 7's implementer.
Task 7: implemented (b2ea5e7), DONE; handoff notes: tests importing roofGeometrySource must vi.mock features/streaming/residentModel; the source freezes the resident snapshot at construction (Task 11 rebuilds per stream version); parseFloat LoD sort undefined for non-numeric labels (minor, deferred). Codex review dispatched over review-26d64a8..b2ea5e7.diff. Task 4: fix round 4/5 dispatched (FRESH Opus implementer; FIX_BASE b2ea5e7).
Task 7: review (Codex) Approved. Minor (deferred): document the resident-snapshot lifetime at the factory; add streaming displacement/fallback, absent-root and surfaces: [] cases; the report overstates which tests assert no measurement.
Task 7: complete (commits 26d64a8..b2ea5e7, review approved)
Task 4: fix round 4/5 implemented (f1fe016, 8e005a4, 52e4e19; fresh implementer). Codex re-review dispatched over review-b2ea5e7..52e4e19.diff. Task 8: dispatched (Opus; BASE 52e4e19).
Task 4: fix round 4/5 (5 addressed, 0 open; commits b2ea5e7..52e4e19; re-review clean)
Task 4: minor (deferred → docs/roadmap): a table build whose statement is in flight at death still holds the FIFO; runQuery callers outside execute (export dialog, counts) await a promise that never settles after a death; refreshLayerTableColumns/retryEngine carry no generation check; runQueue's engine watcher is still transition-keyed (harmless); detection unverified in a real browser (gate smoke: a worker kill via CDP if feasible).
Task 4: complete (commits 75b2221..52e4e19 spread over 5bad30e, b64fd35, 968ecf3, 2a41b3d, bd0b565, bc61764, 801d080, 480b172, d54c6f9, 75e2da5, 26d64a8, f1fe016, 8e005a4, 52e4e19; review clean after 4 fix rounds). Push of Tasks 3–7 after Task 8 lands.
Task 8: implemented (4b1949f, 6d3c0c7), DONE; concern noted for Task 12: Number("") coerces a blanked threshold to 0 — the form must use a range input (or clamp to the default on blank). Push develop 75b2221..6d3c0c7 (Tasks 3–8). Codex review dispatched over review-52e4e19..6d3c0c7.diff. Task 9: dispatched (Opus; BASE 6d3c0c7).
Task 8: review (Codex) Approved. Minor (deferred → Task 12): a blanked threshold input coerces to 0 — the form handles blank intermediate input before storing (range input); parser hardening optional.
Task 8: complete (commits 52e4e19..6d3c0c7, review approved)
Task 9: implemented (351ac3b, 5f7f1e8), DONE; concerns: runQueue.test.ts's "tool with no executor" case now deletes roof-metrics' executor in beforeEach (revisit in Task 13); params normalisation at submit is Task 12's; fake-timer suites must advance the batch yield. Codex review dispatched over review-6d3c0c7..5f7f1e8.diff. Task 10: dispatched (Opus; BASE 5f7f1e8).
Task 9: review (Codex) Approved. Minor (deferred → final review): the CPU-cancel test cancels synchronously from the measurement spy; schedule Cancel as a macrotask at the yield to pin yield-then-check ordering.
Task 9: complete (commits 6d3c0c7..5f7f1e8, review approved)
Task 10: implemented (d373444), DONE (fix: prefix per the brief's message; source+test in one commit under tsc). Push develop 6d3c0c7..d373444 (Tasks 9–10). Codex review dispatched over review-5f7f1e8..d373444.diff. Task 11: dispatched (Opus; BASE d373444).
Task 10: review (Codex) Approved. Minor (deferred → final review): summarise tests lack computed-0, zero-area, root-value/NULL-parts and two-column cases.
Task 10: complete (commits 5f7f1e8..d373444, review approved)
Task 11: implemented (29f1b75), DONE; concern (→ M3 plan): useLodOptions answers only for roof-metrics while ToolView renders the field on needsLod && implemented — the M3 solids tool must extend the hook in the same commit that flips implemented. Codex review dispatched over review-d373444..29f1b75.diff. Task 12: dispatched (Opus; BASE 29f1b75).
Task 11: review (Codex) Needs fixes — Important: switching the target keeps the previous target's LoD when it also qualifies on the new target, instead of re-applying the new target's default (the draft carries `lod` across a targetLayerId change) → the LoD choice is reset (or keyed to its target) on a target change incl. automatic target replacement; regression asserts displayed and submitted LoD. Minor folded: stream-version tests (counts change; the selected LoD stops qualifying). Browser both-themes check → Task 13. Fix round 1 queued behind Task 12's implementer.
Task 12: implemented (b1f7856), DONE; concerns: the slider's caption column (84px) differs from .processing-field's 64px (→ Task 13's browser pass decides: widen the shared column or shorten the caption); PARAMETERS gated on toolId not implemented (harmless); the extension note also fires on `failed` (moot until M3). Codex review dispatched over review-29f1b75..b1f7856.diff. Task 11: fix round 1/5 dispatched (resume implementer; FIX_BASE b1f7856).
Task 12: review (Codex) Approved. Minor (deferred): extension-note visibility and competing-validation tests; the 84px caption column verified visually in Task 13; the note's "on first run" wording beside a failed load (revisit with M3's extension tools).
Task 12: complete (commits 29f1b75..b1f7856, review approved)
Task 11: fix round 1/5 implemented (95f053a). Push develop d373444..95f053a (Tasks 11–12 + the fix). Codex re-review dispatched over review-b1f7856..95f053a.diff. Task 13: dispatched (Opus; BASE 95f053a).
Task 11: fix round 1/5 (2 addressed, 0 open; commits b1f7856..95f053a; re-review clean)
Task 11: complete (commits d373444..95f053a incl. 29f1b75; review clean after 1 fix round)
Task 13: implemented (46f8d77, 885858d), DONE; browser pass on two-buildings + Delft (1,115 buildings, 9.2 s), light/dark, 1000px; caption column unified at --processing-caption: 84px. Concerns → (1) Style by result's median is over ROWS so part rows bias it (M1 Task 13's SQL) — Ruling: fix in the final fix wave: median over root rows (`WHERE "feature_id" IS NULL OR "feature_id" = "id"`, verify the table's convention) — cost if wrong: a biased draft value; (2) the drawer's synthetic "Roof area" and computed roof_area_m2 differ when a feature has both its own roof and parts — correct per §7's contributor rule; Task 14 documents it; (3) app shell overflow below ~1024px — pre-existing, roadmap note; (4) Task 15 tip: two .fcb-url-btn buttons in the Add-layer dialog. Push develop 95f053a..885858d (Task 13). Codex review dispatched over review-95f053a..885858d.diff. Task 14: dispatched (Opus; BASE 885858d).
Codex milestone review pass 'src' dispatched over m2-diff-src.diff at 885858d (tests/docs pass follows Task 14).
Task 13: review (Codex) Approved. Minor (deferred → final review): roofMetricsEnabled test asserts every fieldset/field explicitly; its stale header comment; the report's precedence claim overstated.
Task 13: complete (commits 95f053a..885858d, review approved; browser pass recorded in the report for Task 15's smoke file)
Codex milestone review, pass src (m2-review-src.md): 3 MAJOR, "Not yet". Adjudication — all ADDRESS in M2 fix wave 1: (1) layerTables' build/cleanup awaits never race engine death, so a dropped worker request strands the FIFO despite invalidation → race every build/cleanup engine await against the death signal (the runQueue `raced` helper, hoisted to a shared module), abandon without further SQL, never-settling-request test; (2) Retry renders only for `failed`, but an offline boot leaves lazy extensions `unloaded`, so scenario 6 never shows the download reason/Retry — Ruling: at boot, when `navigator.onLine === false` (advisory), duckdb.ts publishes the two lazy extensions as `failed` with the spec's download reason, so the chips mute and offer Retry; Retry attempts the load, which offline fails again with the same reason; extension-free tools unaffected; an `online` event does not auto-clear (Retry does) — cost if wrong: a muted chip on a captive portal until Retry; (3) the Style-by-result median includes part rows → root rows only (`WHERE "feature_id" IS NULL OR "feature_id" = "id"`, verify the table's convention), test with unequal part counts. Fix wave 1 dispatches after Task 14's docs land; then the tests/docs pass; then Task 15 (verification + smoke).
Task 14: complete (docs 819309c; controller-reviewed: the docs cite code and list the median as open). Fix wave 1 (Codex src MAJORs F1–F3): dispatched (Opus; BASE 819309c).
Codex milestone review pass 'tests' dispatched over m2-diff-tests.diff at 819309c (in parallel with fix wave 1).
Codex milestone review, pass tests (m2-review-tests.md): 3 MAJOR + 3 MINOR, "Not yet". Adjudication: T1 (death tests resolve the blocked request themselves) — covered by F1's required never-settling test, made explicit; T2 (no offline-boot test) — covered by F2's required test, made explicit; T3 (the "streaming" lifecycle test uses a static model) → ADD a real FCB resident-set run test (empty model, resident records with LoD-tagged roofMetrics, published values, feature counts, the resident-set card line, staleness after a rebuild); T4 (cancel scheduled as a macrotask at the yield) and T5 (roofRollUp: a horizontal roof at threshold 0 is non-flat, zero flat area, supplies its azimuth) → folded; T6 (roadmap claims muted chips/Retry unreachable) → one-line docs fix after F2 lands, plus the median line. All forwarded to the fix-wave implementer.
Fix wave 1: implemented (7f8f9c4..af22992, 7 commits: raced hoisted to insights/engineAwait.ts; F1+T1; F2+T2; F3 buildMedianSql root-only; T3–T5 tests; T6 docs; retryEngine keeps an unraced boot wait). Concerns → minor (deferred, roadmap): runQuery callers outside the queue never settle after a death; retryEngine's generation check; the DECIMAL median Uint32Array engine fact only in a probe comment (→ architecture-notes at the gate). Codex scoped re-review dispatched over review-819309c..af22992.diff. Task 15 (gate implementer part: verification, smoke, probe run): dispatched (Opus; BASE af22992).
Fix wave 1: re-review (Codex) — F1, F2, F3, T1–T6 ADDRESSED; no new breakage; retryEngine's unraced boot wait accepted (holds no FIFO task; the owner's unchanged-Retry decision). Push + final whole-branch review after the Task 15 gate lands.
Task 15 (implementer part): complete (b84bf57, 279da34, 4076ec5): vp check 0/56; tsc clean; suite 2983/32 skipped; plugins 840; probes 32; scenario 4 all checks pass on delft.fcb (deviations recorded: Measure solids reads "Not available yet"; Details shows no computed values on a stream — the deferred FCB write-back); scenario 6: warm-engine offline passes; cold offline boot passes via CDP-injected onLine=false; "Retry fails again while offline" UNMET (Playwright offline emulation does not reach the DuckDB worker); engine death driven for real. Noted: one unattributed session where a fresh stream never planned (n=1); `agent-browser set viewport` breaks Navara's canvas. Push develop 885858d..4076ec5. Final whole-branch review (Codex) dispatched over 0519873..4076ec5.
Final whole-branch review (Codex, final-review.md): 0 Critical, 2 Important, 2 Minor, "With fixes". Adjudication — the ONE fix wave: I1 streaming tables rebuild only while the table panel is open, so Tools' scope (from the table) can be zero/stale while its LoD options (from residents) are fresh — Ruling: the streaming-table lifecycle treats the processing toolbox as a table consumer: a streaming layer's table is (re)built on commit while `tablePanelOpen || processingStore.open` (or a run of that layer is queued/running); tests: Tools open with the table panel closed → table built on load and rebuilt on a later commit → the earlier run reads "stale: layer reloaded" — cost if wrong: extra table builds while the toolbox is open; I2 undoRun's awaits and discardUndo's queued runQuery carry no death race → apply `raced` (insights/engineAwait) to both FIFO-owned operations; on death: no further SQL, no model publication; never-settling regressions proving the next queued task settles; m1 the failure reason is not keyboard-visible → reveal the description on :focus-visible (CSS) keeping aria-describedby; m2 architecture-notes still calls the table-build death race future work → correct it (builds race death; the unprotected callers are the export dialog and the counts). Smoke: after I1, repeat the scenario-4 streaming steps with the table panel CLOSED and update scripts/smoke/processing-m2.md (the "prerequisite" wording removed). Dispatch (fresh Opus; BASE 4076ec5).
Final fix wave: implemented (e027894 I1, ad49481 I2, 4bed84c m1, 552d0cc m2, cccf9a4 smoke), smoke re-run with the table panel closed PASS. Concerns: the unconditional sweep on reopening a consumer can retire a finished card as stale (version-aware sweep → M3 follow-up); untracked handoff snapshot dir fails vp check until formatted (mine; formatted at commit). Codex scoped re-review dispatched over review-4076ec5..cccf9a4.diff.
Final fix wave: re-review (Codex) — I1, m1, m2 ADDRESSED; I2 FIFO liveness ADDRESSED, residual open: undoRun's catch of an engine death during the post-commit column refresh still publishes the restored model/provenance and the "Undone" card; computedColumns' inner statement awaits carry no death race (the outer race releases the FIFO; the inner promise is inert).
Final fix wave: parked — "publication after engine death in undoRun (runQueue.ts:973)" — Ruling: the code stands for M2: the Undo transaction COMMITTED before the death (the refresh is post-commit), so the restored model matches the undone table and the "Undone" card is truthful; the death signal has already invalidated the layer's table, so nothing downstream reads the dead engine; the outer race releases the FIFO; an inner never-settling promise is garbage, not a hang. No second fix wave (skill); surfaced to the human as a residual — cost if wrong: a model/provenance publish that is consistent but unverified against the dead engine. Roadmap M3 carries: race the inner statement awaits and short-circuit publication on a post-commit death.
M2 (M13.2): complete — 15 tasks, Codex milestone review (src + tests) + fix wave 1, final whole-branch review + one fix wave, 1 parked residual. Snapshot committed to docs/superpowers/handoffs/2026-09-12-processing-toolbox-m2/; workspace deleted.
