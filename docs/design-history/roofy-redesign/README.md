# Saved redesign context

Preserved verbatim from the `multiroof-viewer-redesign` worktree on 2026-09-08 before its planned deletion: PRODUCT.md, DESIGN.md and the design-panel sidecar.

The active copies are `/PRODUCT.md`, `/DESIGN.md` and `/.impeccable/design.json`. The current workspace was fast-forwarded to redesign commit `73e893d`, bringing the Roofy implementation with it. These saved copies retain the original context for reference; later updates belong in the active files.

Local hook consent, caches, credentials, absolute worktree paths and live-session state were not migrated. Portable live configuration lives in `/.impeccable/live/config.json`.
